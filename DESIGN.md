# Boplet 界面规范

## 网页入口扩展

2026-09-13 语言约定：官网首次访问默认英文，页脚保留明确的“中文”切换；记住用户选择，刷新后不重置。复制指令、跳过导航与 HTML lang 同步当前语言。README 默认英文，顶部提供中文版链接，展示沿用官网品牌色、角色和真实过程稿。

用户对比后最终选择简洁版：去掉案例下方的完整流程条，保留三个真实过程图及精简说明。

首页排版收尾：首屏底部提供指向“开始使用”的文字锚点，不新增复制入口；开始使用标题放大至桌面 42px、手机 34px，两条路径以分隔线区分，复制按钮共用品牌黄色。工作原理保留三个真实过程图，去掉重复图注与五步流程条，每图一句必要说明，末尾只保留方法/工具/用户的分工。

2026-09-13 首页案例采用 Boplet 品牌图标贯穿首屏与工作原理：A/B/C 真实初稿并排、A 双眼改为偏心镂空的前后对比、16/32/64px 定稿检查。三个初稿从 2026-09-11 品牌项目历史只读提取，经共享几何核心渲染为静态 SVG，来源 revision 记在 brand-drafts.js；仅纳入用户授权展示的品牌图形，不包含项目文档或本地路径，不改原项目。删除无关图标横条，改为案例锚点入口；“工作原理”继续位于“开始使用”之后。

2026-09-13 用户确认参考 Superr 的趣味表现和 Craft 的产品展示，仅重设计官网首屏。首页使用金黄色 #f4c653 大色域、墨色 #272319、米白图纸 #fffcf4 与粉色尺寸条 #e9a893；下方两种用法保留原内容，背景 #fcfbf7。首屏左侧 48–76px 系统粗体标语，右侧倾斜图纸展示已确认品牌角色，叠放 16/32/64px 尺寸对比，不产生项目数据。按用户反馈删除演示说明句；构造按钮显示从实际 SVG 路径计算的端点、贝塞尔控制线及轮廓，不使用装饰性的框和节点。反色按钮只切换展示图纸。减少动态效果时关闭悬停动画。所有新规则限定 `.landing-page`，不进入工作区；本段替代下文早期首页灰色 Logo 框、中性配色及标题尺寸的约定。

首页采用“超好用的图标设计工具”作为主标语，保留 Boplet / Icon Studio 和已确认方块角色。该入口面向首次使用者，不沿用编辑器的紧凑信息密度：最大 1120px 内容区，桌面左侧大标语与开始入口、右侧品牌图标和正反色交互展示；600px 以下上下排列。字体仍沿用产品系统字体，标题 40–64px，正文 15–20px；中性浅底与深色主操作，不用新增装饰字体或虚构产品数据。

展示图标明确标为示意，不写入用户项目。下方并列在线 WebMCP 与本地 Skill 两条路径，移动端分段堆叠。官网提供“进入工作区”，指向独立 /studio/。查看工作区只需目录授权，不要求 Agent、WebMCP 或 Skill 加载；设计操作仍要求完整 Skill 与 HTML 同意。本地入口在公开仓库配置前诚实说明使用预览包。鼠标悬停角色轻微转动，减少动态效果偏好下关闭；正反色切换是显式按钮。以上仅适用于网站入口，不改变工作区的布局和字号。

工作区入口使用最大 760px 的独立目录区，提供选择、恢复授权和忘记目录；已有授权直接恢复库及 URL 视图。已打开库保留原工具布局，品牌页脚提供返回官网。断开清除浏览器目录引用并返回入口，不删除项目文件，也不将 Skill 标记为已加载。

产品品牌为 Boplet，副标题为 Icon Studio，中英文均不翻译。品牌区沿用已确认的方块角色图标，名称与副标题上下排列；保留语言切换和文章来源署名。历史文档文件名、工具标识及真实项目名称不随品牌更名。

本文件记录用户已确认的旧容器设计约束，在项目化重建中继续使用；不是重新选择视觉方向。

## 设计意图

工具的视觉中心是图标及其关系，不是产品内部解释。黑白灰、简洁的工作界面；通过位置、尺度和状态表达层级，不用装饰性渐变、蓝色强调、营销内容或卡片堆叠表达“设计感”。使用系统字体是既有产品规范，不引入装饰性标题字体。

项目库 → 项目 → 当前方案图标平铺 → 图标详情。方案高于详情内的局部“图标结构 / 应用场景”切换；它们不是顶层工作模式。左侧切方案，不在图标区域重复方案名。列表条目是导航按钮，无持久选中态。

方案名称按自然升序排列，A/B/C 与数字编号不跟随目录顺序或最近修改时间跳动。需求确认后直接生产首轮图标；方案和规则登记是内部连续步骤，不再呈现第二次参数确认关卡。

## 尺度与颜色

- 页面背景 #f5f5f5，面板 #fff，正文 #222，次文 #616161，分隔 #ddd，控件边框 #909090，选中底 #e8e8e8。
- 无横跨页面的全局顶栏。左侧栏 224px、上下贯通：顶部先是返回箭头和紧凑语言按钮的导航行，下面为项目名；Skill 品牌及文章来源在侧栏底部。返回按钮保留名称与悬停提示，按钮有边界、悬停和键盘焦点状态。中部只保留“项目信息”单一入口和独立方案组。项目信息页面依次显示需求、真实存储位置、运行信息、刷新及断开按钮，不再折叠属性；项目库底部也可查看目录及操作。中央内容从顶端开始；详情按需显示 280px 右侧检查栏。
- 控件 36px 高、6px 圆角、13px/20px 文字；粗指针下最小 44px。卡片 144px 宽、8px 圆角，间距使用 4/8/12/16/24/32px。
- 正文 14px，主标题 24px。状态用中性色、边框、文字，不只靠颜色。键盘焦点保留清晰轮廓。

## 内容归属

- 主方案仅显示紧邻名称的“已确认”文字，其他方案使用现有次文色，悬停和当前查看项恢复正常文字色；不降低整组透明度，不禁用按钮，不改变名称排序。不显示待审核、需修改、待复核。查看高亮与主线确认是两个独立状态，图标修改不移除主线标志。
- 图标列表在“导出”旁提供“复制给 Agent”；结构详情放在右侧“下载 SVG”下，范围为当前尺寸；应用场景提供整个图标的复制入口。成功只短暂显示“已复制”；剪贴板失败才出现可选中的只读指令区域，不新增常驻表单或确认弹窗。

- 语言切换统一归属 Skill 品牌区，紧邻品牌名称：项目库位于底部页脚，进入项目后位于侧栏底部。顶部只保留项目身份与返回导航；项目库不再保留语言专用顶行。此规则替代下文早期关于顶部语言位置的记录。

- 项目库使用独立的无侧栏布局：语言切换在右上角，完整宽度的主内容区显示项目库标题与列表，品牌来源在左下方页脚；页脚随内容自然向下，不覆盖长列表。进入具体项目后恢复项目侧栏、项目身份与对应导航，不能留下一列空侧栏。

- 侧栏顶部辅助按钮常态无边框，保留悬停底色、完整点击区域与键盘焦点。项目名、导航文字和分组标题共用文字左边界；项目信息与方案用间距而非横线隔开，方案标题使用 12px 次文色，减少辅助元素权重。此规则替代早期顶部按钮常态带边框的处理。

- Skill 品牌与项目身份分开：顶部保留返回、语言和项目名；示意图标及产品图标工坊名称放在侧栏底部，附 Inspired by Minor Adventures 与《The Making of Cursor’s Icons》原文链接。品牌区不跟随方案列表滚动、不挤占中央内容；700px 以下随侧栏自然堆叠。来源为方法启发，不暗示官方合作或背书。

- 需求字段固定为设计目的、风格偏好、用户、使用场景、范围、约束。设计目的回答为什么做；风格偏好只记录审美倾向，不预设 Agent 的方案造型。机器合同使用 purpose / stylePreferences，不保留含混的 goals。

- 查看列表：搜索、有效标签筛选、计数、直接导出、平铺与分页。没有数据时是明确空状态，不造演示项目。
- 结构详情：返回列表、图标标题、局部视图切换、尺寸/样式变体、网格/节点/缩放、只读画布、实际尺寸正反色。右侧下载 SVG、图层和属性。节点只是选择，不支持拖动修改；显示点和至少 24px 命中区分离。
- 场景详情：整个语义图标的固定用途、16/32/64/128 尺寸对比和反色；样本说明实际显示尺寸和源变体。无单场景下拉、重复方案标题、免责声明或评价。没有绑定就空状态。点击样本回对应源变体结构。
- 首次需求页（项目尚无方案）左侧保留项目身份、返回入口及项目信息，不显示空方案导航。需求内容栏最大 760px、在主内容区居中；顶部以非交互步骤条显示“确认需求 / 生成方案 / 微调细节”。根据实际需求确认状态标记当前步骤，不伪造确认或生成完成；方案实际创建后恢复方案导航。
- 需求页只读；首次确认标题和引导居中，提示使用“请在输入框中确认需求信息是否正确。如果不正确，请直接对话进行修改。如果需求信息没问题，请回复‘需求已确认’。”进度条不显示“步骤”前缀。正文在所有屏幕宽度统一使用字段名在上、内容在下的左对齐排版，长内容自然换行。用户在聊天补充信息，HTML 不添加创建项目表单、手工几何编辑器或容器内聊天系统。该短语是回复提示，不是网页自动确认命令；确认仍由 Agent 根据用户对当前需求的真实答复执行。
- 更新仅在实际可更新时提示；未知/失败放在可查询状态，不常驻“已保存”。

## 响应式与验收

2026-09-15 用户修正：工作区顶部留白是全局规则，项目库、项目信息、图标列表、结构详情、应用场景以及左右侧栏共用 24px 外边距，不再按文字行框或按钮内部空白分别扣减，不为详情页设置例外。主区及侧栏页脚底部沿用同一 24px 外缘。按钮保留至少 36px、粗指针 44px 点击高度。内部采用 8px 字段标题与内容间距、16px 紧密导航组间距、24px 字段组间距、32px 大区块间距；移动端堆叠后仍使用相同全局顶部规则。

全局主内容使用同一套居中容器规则，以扣除侧栏和检查器后的可用主区为基准，而不是整个窗口。项目库与图标列表最大宽度 1120px，项目信息及需求确认 760px，图标结构与应用场景 1000px；宽屏居中、窄屏保留 16px 边距。标题、工具条、正文和分隔线共享所在容器的边缘。项目库的语言与页脚也遵循同一内容边界。

项目列表使用最大 340px 的紧凑卡片网格，多项目在宽屏并排、窄屏自然换行；日期放在卡片内容下方，不能把单个项目撑成整行。图标列表保持 144px 固定条目宽度，不随余量拉伸。内容限宽不改变侧栏导航、只读交互或原有预览尺寸切换行为。

标题、导航、操作组和长文本保持左对齐；图标画布、实际尺寸正反色对比居中，场景内容栏限宽后在主区居中。不能给整个主区统一 text-align:center。

图标详情的模式切换与标题放在同一行右侧，窄屏不足时自然换行。不同尺寸共用稳定的预览框；实际尺寸样本区按当前图标的最大有效变体预留空间，页面保留滚动条槽位，不能因切换 16/64 px 而让内容区宽度跳动。

图层检查须显示父子缩进、真实独立轮廓和几何属性；布尔运算子图层展示其原始操作数，而非只选中最终合并路径。轮廓包含祖先变换，不写入生产 SVG。矩形不伪造路径节点；路径节点保留键盘选择及焦点反馈，窗口变化时重新计算画布和命中区。

1100px 以下结构属性区域落在画布下方；700px 以下导航横向排列，主内容保持可读，不横向溢出。中文/英文使用同一套控件。宽屏、390px、键盘焦点、源变体选择和工具上下文均以真实浏览器验证；不要把 CSS 数值或构建通过当视觉验收。

2026-09-22：结构详情先显示画布，变体切换与网格、节点、缩放集中在画布下方，空间不足时自然换行。默认 100% 根据画布内容区的宽和高共同适配，最大 520px；手动放大仍可滚动查看。原尺寸浅深底样本保留在控制区下方。

2026-09-22 用户进一步确认：暂时移除画布节点开关、标记及点击入口；底部仅保留变体、网格与缩放，右侧保留图层检查。节点数据和工具合同不删除。

2026-09-22：底部工具栏左侧以轻量按钮切换变体，只显示尺寸及当前集合中存在差异的样式、字重；完整规格保留在悬停提示和无障碍名称中。网格与缩放在同一行右侧，窄屏按组换行。原尺寸浅深底对比独立置于下方。
