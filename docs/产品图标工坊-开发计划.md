# 产品图标工坊开发计划

> 本文保留开发历史。日期记录中的端口、安装路径、包哈希和阶段状态仅描述当时结果；用户入口、开发命令与当前限制以根目录 README 为准。个人主目录已用 `<USER_HOME>` 脱敏，不应直接复制为运行路径。

## 开源准备（2026-09-13）

- 2026-09-14 发布完成：源码提交 `69d1e94d236fc109840233bf4d4357ba94c8ce43` 已推送到 `origin/main`，标签 `v0.1.0-dev.3` 指向同一源码；GitHub Release 已公开为 prerelease。唯一候选 `outputs/release-0.1.0-dev.3-rc1`，build ID `sha256-2c9ccbda8eb612145ad25583aef9983929faec57c3eef47e5c6ea61e922f8e6b`，ZIP SHA-256 `1b88b934de93ecc7c380fe759144b7f669bf8941f7b3898ae746bde8a8f43a03`，293558 字节。候选校验与隔离安装/启动/导出/离线重开/卸载保留数据验收通过；公开附件匿名下载后哈希及字节数一致。
  - 官网复用同一候选构建 `outputs/web-0.1.0-dev.3-release`，Wrangler 4.131.1 部署版本 `23cbbad6-334c-41d2-aa09-d17356bc9255`。线上首页、工作区 HTML、JS/CSS、Worker、下载清单与 Skill 说明及版本逐字节一致；真实 Chrome 中英文切换、1280/390px 和工作区入口通过，无页面异常。截图 `.qa/release-dev3-live-1280.png`、`.qa/release-dev3-live-390.png`、`.qa/release-dev3-studio.png`。
  - `www` 实测 308 且保留路径与查询参数，既有独立 Worker 无需重复部署。恢复后的 `53058` 预览返回 200，PID 10653 保留；旧 `55822` 服务保留。验证浏览器和隔离包服务已关闭。未修改真实项目、全局 Skill 安装或原 `dev.2` 附件；Windows 实机及完整外部目录权限矩阵仍待验收。

- 2026-09-14 发布收尾：用户授权恢复预览、提交推送并发布本轮改动。`53058` 已恢复响应；版本递增为 `0.1.0-dev.3`，为共享界面准备新的完整 Skill 候选，保留原 `dev.2` 附件。发布回归 86 项通过；12 项浏览器回归首次 11 项通过，本地 HTTP 布局读取一次遇到瞬时空节点，未改产品代码，单独重跑 HTTP 与原生 WebMCP 两条完整流程均通过。桌面及手机目录汇总、官网实际渲染已复核。实际发布与部署回执在完成后补记。

- 2026-09-14：网页目录识别已支持项目库或父目录，父目录只扫描下一级并汇总可识别项目，卡片标明所属库；选择空目录、项目数据目录、旧格式、损坏数据分别反馈，不自动写入或迁移。跨库打开时切换单库 Worker，URL 保留库路径并支持刷新及浏览器返回，重复项目 ID 不串库。只读核验用户授权父目录，识别到一个当前格式库内的两个项目及两个旧格式目录。86 项单元/合同/集成测试、查看器与在线工作流浏览器回归通过，覆盖空库、跨库、取消、无写入、重开与导出，中英文桌面/手机渲染已检查。已更新本地网页构建与 `53058` 预览；未部署、未提交，未替换旧本地容器或已安装 Skill。

- 2026-09-14：修复网页未连接项目库时“连接已有目录”重复读取原目录的问题，改为在用户点击中直接打开目录选择器；取消保留旧选择，成功切换后重新绑定运行时并刷新项目库，不重复挂载页面。空状态展示目录名、已有项目定位要求、空目录创建指引，按钮使用独立操作区。合成目录浏览器回归覆盖空目录无写入、取消、切换成功、刷新和导出，桌面/手机渲染已检查。旧 `55822` 属于独立的历史本地容器，未替换其构建或更改真实项目；新网页预览保留在 `http://127.0.0.1:53058/studio/`。未部署，父目录发现逻辑未包含在本次修复中。

- 2026-09-14：官网能力提示补充 Codex 内置浏览器使用指引、版本/模型/设置条件及官方支持说明链接，中英文同步。将“目录授权”改为“文件夹访问能力”，不把未检测到 API 误报为用户未授权；保留本地 Skill 备选。依据 `https://learn.chatgpt.com/docs/webmcp` 核实支持边界。首页浏览器回归与构建通过，桌面/手机渲染已检查；本次未部署。

- 2026-09-14：按用户授权新增 `www.boplet.app` 独立跳转 Worker，版本 `3fac0005-3296-46a8-9016-89fbe69e1cb8`。DNS 公共查询已返回正常记录；直连解析所得 Cloudflare IP、保留域名 TLS 校验，HTTP 与 HTTPS 均实测返回 308，路径及查询参数保留。原主站部署未改变。本机代理解析仍有旧失败缓存，未修改用户代理配置；未提交 Git。

- 页脚调整已按用户授权部署：复用原 Skill 候选，网页输出 `outputs/web-0.1.0-dev.2-footer`；Wrangler 4.131.1 部署版本 `a5198b7e-ed38-4c22-9250-7b9efd12baf3`。线上脚本、样式、下载清单与产物一致，桌面/手机页脚分层、GitHub 入口及语言切换验证通过，验收浏览器已关闭；未提交 Git。

- 官网页脚改为两层：品牌居左，GitHub 与语言切换居右，灵感来源独立置于下层；字号与间距同步调整，手机端文章链接换行。仅作用于官网，工作区共享页脚保持原样。新增入口与分层布局回归先失败后通过，网页构建通过，桌面/手机真实浏览器渲染已检查；本次未部署、未提交。

- 根据用户反馈删除本地安装按钮下“目前通过预览包安装”及英文提示，不替换成 GitHub 渠道说明。安装指令、下载清单和已发布 Skill 包均不变。首页回归通过，Cloudflare 部署版本 `191ab1d6-7899-4db7-8977-a2b66f0191d3`；线上脚本逐字节一致，中英文桌面/手机已验证提示消失并检查渲染。

### 发布前检查与候选验收

- 用户授权完成发布：仓库已 PUBLIC；`v0.1.0-dev.2` 标签指向候选源码 `1047ebe0e8e006330bbe556e43e9c1008b1589dc`，GitHub Release 已从草稿发布为 prerelease，包含完整 ZIP、release.json、sha256.txt。未使用 npm 分发。
- 无凭据匿名下载公开 ZIP，SHA-256 与原候选一致，并对实际下载解压包运行隔离安装/浏览器连接/合成图标导出/离线重开/卸载保留数据回归，全部通过。临时测试目录与浏览器/服务均已收尾，未更改实际 Skill 或真实项目。
- Cloudflare Wrangler 4.131.1 部署同一候选准备的网页到 boplet.app，版本 ID `646673fe-9531-4519-baaf-5b98934b9096`。线上首页、app.js、style.css、工作区 HTML、Skill 及运行说明、下载清单均 HTTP 200 且与产物逐字节一致，清单指向公开 GitHub ZIP。
- 真实线上 Chromium 验证英文默认、中文切换及刷新记忆、复制本地指令、进入 /studio/；页面渲染检查完成。README 双语版更新为已可用 GitHub Release；当前仍为开发预览，Windows 与完整授权矩阵等原有限制不变。

- 用户已授权清理历史个人路径：使用 git-filter-repo 2.47.0 仅重写本地 main 的 Markdown 主目录前缀。17 个提交逐一核对，28 个文档历史版本仅做指定替换，作者、日期及其他文件保持不变；清理前后 HEAD 文件树完全相同。main 全历史个人文档路径复查为 0，Gitleaks 再扫未发现密钥。
- 原始完整 bundle 与提交映射保存在仓库外的私有备份目录，不提交、不发布。远端跟踪引用、Codex 内部检查点及本地旧对象未删除；后续只推送明确分支，不使用 --mirror/--all。远端历史尚未清理，需在后续推送授权中使用明确分支及 force-with-lease，不普通强推。
- 候选源码旧提交 `138b835` 映射为 `1047ebe0e8e006330bbe556e43e9c1008b1589dc`；旧验收提交 `950282d` 映射为 `9da1b00`。文件内容和已验收候选哈希未变，无需重打包或重跑业务测试。以下旧哈希保留为历史对照。未推送、公开或部署。

- 代码已本地提交 `138b835`。从该干净提交以 Node 22.23.1 生成候选 `outputs/release-0.1.0-dev.2-rc1`（仍是 development preview）；未创建/推送 tag。
- 候选 build ID：`sha256-69cbc2a1cc2b3f6c6986960ad367123cf70a0796dab40ff6cf3476980f343a74`；ZIP SHA-256：`a1d18b7ab0c1662ff6c2ca2dbafcd3a4a61950031ed17b338d72087c5c8f6d32`，291619 字节。check:release 通过。
- 指定上述候选运行 BOPLET_TEST_SKILL 回归，通过临时安装、浏览器真实连接、合成图标编译/SVG 导出、停止后离线重开、卸载保留独立数据。测试服务及临时目录收尾完成，候选保留；未替换实际安装、未碰真实项目或既有预览。
- GitHub 当前查询为 PRIVATE，未推送、公开、发布 Release 或部署。历史隐私处理等待用户决定；上述 GitHub 下载清单仅为未来发布地址，尚未验证可公开下载。本条记录提交只含验收文档，候选身份仍绑定 `138b835`。

- Gitleaks 8.30.1 对本地 `--all` 可达的 15 个提交及当前 113 个源码/文档文件扫描，未发现密钥；不是对未知远端引用、外部附件或所有隐私风险的保证。扫描报告仅留在忽略目录 `.qa/`。
- 历史 README、开发计划和架构方案仍有个人机器路径，Git 历史有作者身份信息。当前文档路径已脱敏，但历史未重写；公开前需确认是否接受历史信息或另行授权历史清理。不能将密钥扫描通过表述为所有公开风险已消除。
- 增加 `BOPLET_TEST_SKILL` 候选验收入口，复制指定完整包到临时安装，沿用实际浏览器/HTTP、合成图标导出、离线重开和卸载保留数据回归。不修改候选、全局安装或真实图标目录。

### 开发、本机测试与 GitHub 分发管线整理

- 维护入口统一为[开发测试与发布](开发测试与发布.md)：区分源码预览、隔离浏览器测试、候选安装、本机实际更新、GitHub Release 和官网部署；README 双语版及贡献指南链接该文档。npm 仅管理开发依赖，不作为 Skill 分发渠道。
- 新增 prepare:release / check:release，输出完整 Skill、确定性 ZIP、SHA-256 与 GitHub 下载清单；校验版本标签、内容和哈希，拒绝覆盖非空候选。没有执行发布。
- 打包共用 archiveSkill；pack:skill/check:package 支持 --output。prepare:web 支持 --skill-release 和 --output，复用验收候选及在线 Skill 说明，不另建第二份安装包。默认预览入口保留，GitHub 可公开下载后才切换官网。
- 本机 install/check 支持明确候选目录，不再只能构建当前源码；损坏候选拒绝安装，已有安装保持原样，构建失败清理本次临时目录。实际全局安装、备份和数据未改。
- 验证：83 项单元/合同/集成测试通过，新增 ZIP/清单/官网一致性及损坏候选保护回归。完整浏览器回归原有 11 项通过，另一项因英文默认入口导致旧中文定位失效，已修正并单独复验。Skill 元数据校验与 diff 检查通过。无提交、推送、公开、部署；本次测试进程已结束，既有预览保留。

- 官网首次访问默认英文，保留中文切换及选择记忆；指令、跳过导航与 HTML 语言属性同步。工作区已有语言逻辑未改。
- 重写英文 README，顶部明显链接中文版；使用官网品牌图与真实 A/B/C、A 微调过程图。补充两条使用路径、本地数据边界、开发命令及预览限制，移除过时的“尚未开放在线流程”等说明。
- 用户确认 MIT：新增 LICENSE、npm 许可证元数据，Skill 构建白名单和清单包含许可证。补充贡献指南、第三方许可证及灵感来源说明；忽略 Cloudflare 本地缓存与变量文件，脱敏账本个人路径。
- 验证：82 项单元/合同/集成测试通过；首页浏览器回归覆盖英文首次访问、中英文切换及刷新保持、英文复制指令和多视宽。英文桌面/手机与 README 配图实际渲染已检查，本地链接与网页构建通过。
- 当前仅完成源码和文档准备，未提交、推送、部署或公开仓库，未修改全局 Skill 或真实项目。公开前仍需审查 Git 历史中的敏感内容并确认发布范围；当前文件整理不等同完整历史安全审计。跨平台及目录授权矩阵的待验收项仍保留，不宣称正式版就绪。

版本：1.6（WebMCP 网关与本地安装一致性，需求边界不变）。日期：2026-09-11。状态：A0–A3 自动化及本机核心验证完成；A4-01、A4-02 已关闭，Windows 实机仍未验收。

唯一需求基准：[《项目化架构与双端产品方案》v1.0](./产品图标工坊-项目化架构与双端产品方案.md)。需求已确认，本计划只确定实现顺序、工作包和验收方式，不重新展开产品决策。

本计划编制后，已建立用户指定的独立 Git 仓库，需求和计划以仓库 docs/ 为唯一版本。用户随后授权持续推进到 A4；当前已落地共享核心、本地容器、两条操作路径及候选包，并完成本机隔离运行。未替换全局 Skill、未修改旧预览或真实项目、未部署网站。需求边界未改变，状态与剩余门槛见 §10.2。

## 1. 实施结论

分为两个交付阶段：**先完成可独立使用的 Skill，再发布共用核心的网页版。**

阶段一内部先交付一条最小但真实的完整流程：空目录 → 草稿项目 → 聊天补充需求 → 确认 → 首个方案 → 登记图标与变体 → 生成 → 容器查看 → 导出 → 重新打开。随后补齐规模维护、全部几何操作、任务恢复和发布质量，不能把这条最小流程当成完整版本提前交付。

WebMCP 和无 WebMCP 的本地入口从第一个可执行工作包起共用操作定义与业务实现；网页端文件适配后置，浏览器可运行的核心设计前置。

```text
A0 合同与工程骨架
 → A1 共享核心、文件存储、本地运行基础
 → A2 首个项目的完整设计流程
 → A3 完整图标系统维护能力
 → A4 Skill 打包与两条路径验收
 → 阶段一交付
 → B1 浏览器本地目录适配
 → B2 无本地服务的在线运行
 → B3 首次安装引导与 Skill 更新分发
 → B4 双端接续、部署验收
 → 阶段二交付
```

默认由当前 Agent 顺序实施，不将启动子 Agent 或多人并行作为计划前提。这里的“多个 Agent 场景”是产品验收场景，可用两个测试客户端复现，不意味着自动创建开发子任务。

## 2. 不可改变的实施边界

| 已确认要求 | 开发中必须体现 | 禁止偷偷加回的实现 |
| --- | --- | --- |
| 先安装并加载 Skill，再选择入口 | 安装/加载就绪是正式工作流起点，Skill 给出标准操作说明 | 让模型从零研究全部 MCP；把下载 ZIP 算作安装完成 |
| WebMCP 优先，不是门槛 | 同一操作定义同时接 WebMCP 和本地内部接口 | 无 WebMCP 只能预览；另造功能缩水版 |
| 在线不运行本地服务 | 核心可在浏览器执行，浏览器直接读写授权目录 | 公网页面绕回 localhost；把 Python 文件 API 暴露到公网 |
| 数据与容器分离 | 项目写入用户目录，安装包只有程序和规范 | 打包真实 HETAO 项目、日志、令牌或缓存 |
| 多项目 → 多方案 → 图标 → 变体 | 显式身份和目录边界，方案默认不混排 | 将旧 `projectKey` 继续冒充项目 ID |
| 只封存，不迁移 | 新版本新建数据，旧目录保持原样 | 旧 ID 映射、旧 URL 适配、项目格式转换器 |
| 后保存生效 | 同一目标最后一次成功写入的内容生效 | 项目占用、租约、`expectedRevision` 拦截、自动合并、冲突裁决 |
| 无手工设计表单 | 聊天写需求；容器负责查看、状态与审核 | 新建项目填写表、手工几何编辑器、容器内聊天产品 |
| 下载/导出可手动点击 | 工具与按钮调用相同交付能力 | 只剩工具、按钮入口被误删，或单项导出下拉 |
| 检查 Skill 更新 | 自动检查、可查询、去重提醒、不自动安装 | 常驻更新守护进程、强制升级、自动覆盖当前包 |
| 轻量、可本地离线 | 本地运行资产随包携带，不依赖 CDN | 云端业务数据库、账号系统、多人协同平台 |

## 3. 当前代码基线与复用策略

本节是编制计划时对旧候选的只读基线，不是新版当前状态。新版实现与当前测试证据见 §10.2；旧候选仍保持原样。

旧候选在原临时工作区的 `outputs/make-product-icons/`，测试主要在该工作区的 `work/`。本节旧源码路径均以 `<USER_HOME>/Documents/Codex/2026-08-16/https-www-minoradventures-co-blog-the` 为基准，具体位置见仓库 README；它们不在新仓库的父目录下。没有在该候选目录发现 `package.json` 或 README。它仍是 Python 本地服务配合原生 JavaScript 页面，不是可以直接静态部署的完整产品。

| 当前资源 | 后续处置 | 原因 |
| --- | --- | --- |
| `assets/studio/index.html`、`studio.css`、`DESIGN.md` | 复用已确认结构、样式和组件尺度 | 不重做配色、标题层级、图标列表/详情关系 |
| `dropdown.js`、`preview.js`、`context.js`、`workspace.js` | 按新身份和状态合同改造可复用逻辑 | 保留键盘交互、选区与结构查看能力，不保留旧字段别名 |
| `studio.js`、`webmcp.js` | 分离视图和传输，按新操作定义接入 | 当前工具与 `/api/*`、旧身份和版本拦截耦合 |
| `matrix_geometry.py`、`vector_path.py`、`compile_matrix_icons.py` | 作为几何语义参考，重建共享 JS/TS 核心 | 本地与网页需要同一个编译实现；不是旧项目转换 |
| `studio_session.py`、`studio_collection.py`、`studio_schemes.py` | 提取业务意图，不整体包一层当新后端 | 仍有 `expectedRevision`、`fcntl` 项目锁、来源方案依赖与全量 gallery 编译 |
| `studio_http.py`、`serve_matrix_studio.py` | 参考本地安全边界和启动流程，重建薄服务 | 新版从空 Library 启动，不能依赖既有项目或 Python 生产引擎 |
| `work/test_studio_context.mjs` 等 JavaScript 测试 | 迁入对应模块测试并改为新合同 | 保留正确的交互回归，废弃旧 scope/别名的期望 |
| `work/test_studio_v2.py` 等几何测试 | 将有价值的数学断言用合成数据移植 | 保留曲线极值、边界、光学校验等，不能用真实项目做发布夹具 |

共享几何是主要技术工作，不应被算成“换几个 API”：当前 `matrix_geometry.py` 有 810 行，此外还依赖路径运算模块。布尔运算、曲线极值和描边边界优先验证，不能只验证一个矩形后开始网页封装。

现有全局安装目录和旧预览不作为开发写入目标。历史封存采用原地保留，不建设封存工具、不移动或清理用户项目。

## 4. 工程组织与接口划分

### 4.1 独立开发目录

独立 Git 仓库已按用户指定移至 `<USER_HOME>/GitHub/icon-studio`，作为下一版单一工程。后续直接在仓库根目录实施，不再创建嵌套的 `icon-studio/`。下面是目标结构；当前已创建 contracts/core/storage、合成测试和合同构建入口，其余按工作包逐步建立。旧候选包保持原样，避免改到正在使用的预览来源。

```text
icon-studio/
  README.md                    # 安装、开发、构建、测试和支持环境
  package.json                 # 一个工程，不拆成多个发布包
  src/
    contracts/                 # 数据 schema、操作定义、错误、能力清单
    core/                      # 项目、需求、方案、任务、几何、审核、导出
    storage/                   # 接口 + memory/node/browser 适配
    runtime/                   # 本地服务、启动器、浏览器 Worker
    transports/                # WebMCP 注册、本地内部操作分发
    app/                       # 一个 HTML 应用入口、路由、组件、样式
  skill/
    SKILL.md
    references/                # 标准操作、格式、设计方法、两条完整示例
  tests/
    unit/
    contract/
    integration/
    e2e/
    fixtures/                  # 仅合成数据和模拟发布清单
  scripts/                     # 构建、测试、打包；不是业务 CLI 产品
  release/                     # 版本与发布资产模板，不放用户项目
```

候选产物输出到 `outputs/icon-studio-next/skill/` 与 `outputs/icon-studio-next/web/`，由同一版本构建生成。打包时技术调用名仍是 `make-product-icons`，不因开发目录名另造一个 Skill。安装/正式部署放在交付门槛之后单独执行，不覆盖现有全局安装来验证开发中间态。

### 4.2 关键实现约束

- 采用需求文档确定的 JS/TS 共享核心、本地 Node 运行时、浏览器 Worker。具体受支持运行时和构建依赖在 A0 固定并记录；不在计划中臆测未来最低版本。
- 不强制引入新的 UI 框架，优先保留现有原生组件。核心不导入 DOM、Node 文件 API 或浏览器目录句柄；这些依赖留在适配层。
- `operations` 是唯一可执行操作定义源：参数 schema、结果 schema、注解、处理函数、工作流阶段和权限需求均在这里。WebMCP 与本地内部接口只做传输适配。
- 本地接口基线为读取能力目录、调用具名操作、读取会话上下文三类入口。Agent 按 Skill 示例用宿主已有 HTTP/执行能力调用，不新增业务命令行应用或 MCP 服务器。
- `Storage` 只提供授权范围内的读取、枚举、完整文件写入和基本文件操作；不加入 acquireLock、compareAndSwap、merge 等方法。
- `sourceRevision` 只用于说明来源；内容哈希用于预览缓存/审核关联，不作为“别人改过所以拒绝保存”的检查。
- `expectedContextId` 只识别本会话用户指的是哪个对象。另一个 Agent 写了同一文件不导致项目独占或冲突拒绝；目标已不存在仍返回正常的目标错误。
- 确认记录绑定用户确认的内容，不伪造“确认过新内容”。正常更新 brief 不因为另一会话存在被禁止。
- 多文件提交只报告逐项结果，不建设跨文件事务或全局事件流。单会话可去重，同目录跨进程不承诺恰好一次执行。

## 5. 阶段一工作包：完整 Skill

工作包定义保留原验收要求；当前状态统一看 §10.2，不把定义中的未来语态当作仍未实现。每个工作包只有取得对应证据才能标记完成。

### A0：工程骨架、数据与操作合同

**交付**

- A0.1 建立上述工程、README、构建和非 watch 测试入口；记录依赖许可证与支持环境。
- A0.2 固定 Library/Project/Scheme/Icon/Variant、brief、rules、task、review、delivery 的 schema，区分项目身份、方案身份和内容版本。
- A0.3 建立具名操作注册表、统一结果与错误；按需求 §11.2 覆盖所有能力，不仅列工具名称。
- A0.4 建立 memory Storage 与合成夹具生成器：空库、零方案项目、两个同名但不同身份项目、多方案、不同尺寸/样式/轻重。
- A0.5 明确当前版本格式，不支持旧格式；用测试禁止项目锁、旧 ID 别名和基础版本拦截进入新写入链。

**先写的失败测试**：空项目可序列化；项目/方案 ID 不混用；非法嵌套字段拒绝；来源版本较旧不是写入错误；返回 schema 能表达实际失败。

**完成证据**：构建与合同测试可运行；操作覆盖表没有未登记能力；新目录不触碰旧项目；没有凭空宣称线上或安装可用。

### A1：共享几何基础、本地存储与运行入口

依赖 A0。

**交付**

- A1.1 先验证高风险几何：路径节点/控制柄、曲线极值与墨迹边界、圆角、布尔运算；再补基础图形。需要算法依赖时只选择可在 Node/Worker 同用、许可证可分发的实现；不维持两套生产引擎。
- A1.2 实现 Node Storage：默认系统文稿位置、用户自选/当前目录、配置与项目分离、完整文件写入、不兼容格式识别。
- A1.3 实现内置启动器：检测和复用本任务服务、动态端口、版本与健康回执、明确退出/保留规则。启动器不自动载入或改写旧 HETAO 项目。
- A1.4 提供薄本地服务、受控内部调用和 WebMCP 适配骨架；安全保留回环地址、Host/Origin、会话凭据、路径与请求大小限制。
- A1.5 加入会话内操作状态和有界事件读取；取消只针对指定会话/操作，不关闭其他用户会话。

**先写的失败测试**：拒绝越界路径与未授权读取；保存失败返回 failed；两个客户端顺序覆盖同一目标，第二个旧副本成功并生效；几何边界与实际输出一致；空目录可以启动。

**完成证据**：同一核心能在 Node 和浏览器测试环境运算合成几何；本地服务从空库运行；两条入口对同一操作得到同构结果；无项目占用机制。

此处浏览器环境仅验证共享核心能运行，不提前完成线上部署或浏览器文件存储。

### A2：从空项目到单图标交付的纵向流程

依赖 A1。这是第一个可供用户完整体验的开发里程碑，不是完整 Skill 发布。

**交付**

- A2.1 项目创建/列表/读取/打开/更新；零方案是正常状态；页面默认不抢走当前浏览目标。
- A2.2 聊天写 brief、来源分类、缺项检查、确切内容确认；需求页只展示信息，不增加表单。
- A2.3 建立首个方案，不需要 sourceSchemeId；登记语义、tags、用途、变体与首批任务。
- A2.4 使用共享核心写入一个合成图标、校验、编译；明确区分 draft、compiled、review 与 persisted。
- A2.5 接入现有 HTML 外观：项目库 → 项目 → 方案列表 → 图标详情；显示最新预览、图层/节点选区。
- A2.6 下载 SVG 和导出按钮与工具共用执行逻辑；重新打开能读到保存的项目。
- A2.7 同步编写 Skill 的“从空目录开始”示例，分别列出 WebMCP 和本地调用顺序，不等最后才补说明。

**先写的失败测试**：未确认需求不能生产；空项目能建首方案；用户在聊天补充后页面相同字段更新；图标用途与尺寸变体不混淆；工具与按钮导出同一版本；重开不重复创建项目。

**完成证据**：同一个合成用例分别通过 WebMCP-only 与禁用 WebMCP 的本地调用走完，保留操作记录、磁盘结果和真实页面截图。

### A3：完整维护链与大图标库

依赖 A2。按下面顺序推进，每组完成即接入两条入口与现有页面。

| 顺序 | 工作包 | 关键验收 |
| --- | --- | --- |
| A3.1 | 项目/方案归档恢复、方案复制、规则保存、偏好方案；来源资料读取 | 复制是独立实现；原目录授权不延伸至其他目录；查看不等于采用 |
| A3.2 | 完整几何与组件操作、节点、分组、布尔、复用、校验和编译 | 需求规定的几何操作全部可用，不为网页方便删掉复杂操作 |
| A3.3 | 新增图标/变体/批次，真实任务进度、暂停/取消/续跑 | 不停服登记；不会把未执行动作写成成功；取消不撤销已保存的其他结果 |
| A3.4 | 当前上下文、图层与控制柄读取，导航与显示设置 | 同名图标不串项目；隐藏的画布缓存不冒充当前场景选区 |
| A3.5 | 固定应用场景、用途绑定、真实尺寸与反色预览 | 全部相关场景展开，无评价、重复方案名或单场景下拉；缩放不是新变体 |
| A3.6 | 反馈、审核、历史列表、比较、撤销/重做与显式恢复 | 接受绑定内容版本；恢复是新保存；不建设多人合并历史 |
| A3.7 | 项目/方案/图标/变体导出，源与产物追踪 | 范围与内容可验证；不把保存、编译、审核、导出混为一谈 |
| A3.8 | 名称/concept/tags 查询、后端分页、缩略图缓存、按需编译 | 不能先编译全部再分页；未打开项目不编译；有效标签才显示筛选 |

**完成证据**：完整工具覆盖测试、真实任务恢复记录、600 图标夹具下的查询与渲染测量，以及既有 UI 设计回归截图。

### A4：Skill 更新、分发与阶段一验收

依赖 A3。

**交付**

- A4.1 实现 `check_skill_updates` / `get_skill_update_status`，两条入口均可访问；24 小时成功缓存、显式重查、同版本提醒去重、失败不阻塞。
- A4.2 使用固定发布清单 schema 与受控来源；当前 Skill 版本未知时不得报告最新；不自动下载安装、不热替换运行中的核心。
- A4.3 整理完整 SKILL.md、设计方法、操作合同、启动/恢复/目录规范、两条路径示例和“后保存可能覆盖”的简短说明。移除“必须原生 WebMCP”、业务 CLI、旧格式兼容等相反指令。
- A4.4 同一构建生成 Skill 包与版本清单；所有本地运行资产随包提供，不依赖 CDN。
- A4.5 执行发布白名单打包和敏感/真实数据扫描；只用合成夹具，测试目录不作为用户内容混入包。
- A4.6 在隔离安装位置验证安装后启动、重新调用、断网、更新检查和卸载程序不删除用户项目。确认安装成功与加载成功分别留证。

**完成证据**：macOS、Windows 各自的真实运行记录；两条完整 Agent 操作路径；合法重复保存、保存失败、断网和重开证据；可复现的包哈希与构建版本。

**阶段一门槛**：A0–A4 均完成，才标记“完整 Skill 可交付”。合成发布源测试只能证明检查逻辑；真实更新源尚未部署时必须标记尚未接通，不声称自动更新提醒已在线运行。

## 6. 阶段二工作包：直接在线使用

阶段二只增加浏览器存储、运行装配、首次使用与发布环境，不复制业务实现。阶段一通过后再开始正式阶段二；A1 的浏览器核心测试不算网页版交付。

2026-09-12 用户明确授权提前推进网页版开发：正式域名为 `boplet.app`，用户已在 Cloudflare 购买。本次先推进 B1，保留 A4 Windows 实机未验的缺口，不将其改记为完成。域名归属来自用户确认；本仓库尚未配置 DNS、托管或生产部署。

### B1：浏览器目录存储

- 实现与 Node Storage 同合同的目录句柄适配，用户交互完成授权，清晰报告 pending/granted/denied/cancelled。
- 默认目录只是选择建议，不能静默代替用户授权；句柄与偏好可存在浏览器，正式项目必须在用户目录。
- 重开、重新授权、更换目录、清站点数据后重新选择原目录，按 libraryId/projectId 找回内容。
- 无目录 API 或权限失败不转存隐藏缓存；引导使用已安装 Skill 的本地容器。
- 完整文件写入和失败回报；多窗口不加占用或基础版本门槛，遵守后成功保存生效。

**完成证据**：对同一合成项目，Node 与浏览器适配读到相同数据；浏览器实际授权、取消、撤权和重连录像/截图及结果记录。模拟句柄单测不能代替真实权限验收。

### B2：不依赖本地服务的在线容器

- 从同一源构建静态站点，Worker 执行核心，浏览器 Storage 落盘。
- WebMCP 使用相同操作定义；本地内部 HTTP 入口不打包进在线执行依赖。
- 检查页面、网络和进程证据：在线流程没有启动 localhost，也没有发送项目数据到网站后台。
- 原生 WebMCP 不可用时说明实际能力并转本地；不安装伪造的 API、不用页面自动点击补齐业务。

**完成证据**：测试环境仅提供静态资源，完成项目创建至导出；在线调用和本地调用对合成输入产出一致；浏览器主线程在大库读取/编译时仍能响应导航。

### B3：先安装 Skill 的首次使用与分发

- “开始设计”入口提供标准宿主指令，先安装/加载 Skill，再选择在线或本地。不以页面猜测本机文件代替安装证据。
- 完整 Skill 包、版本、校验值、标准说明随站点提供。已安装且可用则复用，不每次重装；不静默覆盖用户现有安装。
- 初始只读引导可返回安装来源、要求版本和状态；正式操作在 Skill 就绪后开始。宿主转述必须标明证据来源，不包装成人类签名或强安全认证。
- 将 A4 的更新能力连接真实固定发布清单；清单与下载包版本/哈希保持一致，旧源失败有明确状态。
- Skill 已加载的在线用户不启动本地服务；在线出现问题才按用户选择启用本地。

**完成证据**：一个未安装用户的完整在线开始流程；一个已安装用户复用流程；没有 WebMCP 的用户转本地完成全流程；不支持自动安装的宿主有明确下载/加载步骤，而不是死循环重试。

### B4：双端接续与部署验收

- 在线保存 → 本地选择同一目录继续 → 在线重新读回；保留项目/方案/图标身份，不复制、不导入、不迁移。
- 正常顺序接续验证已保存内容；异常退出只恢复已完整落盘的数据，不承诺找回内存草稿。
- 两端同时打开、先后写入同一文档，后保存生效；不要求释放旧会话，也不承诺字段合并或跨文件全局顺序。
- 静态资源和 Worker 使用明确构建版本，避免旧 HTML 加载新核心；程序回滚遇到不支持的格式只拒绝写入，不转换数据。
- 检查 HTTPS 部署、目录授权所需环境、CSP、下载链接、发布清单与缓存策略；不得增加账号或云端项目服务。

**完成证据**：双端接续报告、两个入口的实际文件内容、发布资产清单、真实站点冒烟、版本不匹配测试和可执行的程序回滚记录。

**阶段二门槛**：B1–B4 均通过且实际部署完成，才宣称网页版上线。测试站点通过、本地构建完成、公开网站上线分别记录。

## 7. 全流程能力归属与防漏清单

所有能力都进入共享操作目录，不在 WebMCP 包装层单独实现业务。下面对应需求 §11.2 的全部能力域。

| 能力域 | 实现工作包 | 在线补充 | 完整验收关注点 |
| --- | --- | --- | --- |
| 发现、工作流引导 | A0、A1、A2 | B3 | 能读 schema、入口、安装状态与门禁，而非只列名字 |
| 会话、当前视图、选区 | A1、A2、A3.4 | B2 | 精确到项目/方案/图层/节点/控制柄，区分隐藏缓存 |
| 导航与显示设置 | A2、A3.4 | B2 | 每个用户查看行为均可读、可调用 |
| 存储、目录、索引扫描 | A1 | B1 | 授权范围、拒绝、重连，不扫描整个磁盘 |
| 项目 | A2、A3.1 | B1/B2 | 零方案、多项目、归档恢复、同名不混淆 |
| 需求与确认 | A2 | B2 | 对话写入、来源区分、确认绑定内容 |
| 来源资料 | A2、A3.1 | B1 | 单独只读授权，不执行仓库代码 |
| 语义与元数据 | A2、A3.3 | B2 | concept/tags/用途真实登记，退役可恢复 |
| 方案 | A2、A3.1 | B2 | 无来源首方案、独立复制、选择不等于采用 |
| 规则与影响 | A2、A3.1/A3.2 | B2 | 规则保存、影响枚举，不能静默改全库 |
| 变体 | A2、A3.3 | B2 | size/style/weight 与缩放、用途分离 |
| 计划与批次 | A2、A3.3 | B2 | 运行中登记，明确实际目标 |
| 任务 | A3.3 | B2 | 真实结果、暂停、失败、取消与续跑 |
| 图标查询与读取 | A2、A3.4/A3.8 | B2 | 数据端分页、草稿/生产版本清晰 |
| 组件与几何 | A1、A3.2 | B2 | 所有已要求操作，同一算法 |
| 验证与编译 | A1、A2、A3.2 | B2 | 不直接修改 SVG，不把校验通过算审核通过 |
| 固定场景 | A3.5 | B2 | 固定预设、真实源变体，无场景空态 |
| 反馈与审核 | A3.6 | B2 | 用户原意、明确版本、修改后旧审核失效 |
| 历史 | A3.6 | B2/B4 | 顺序使用下可读、比较、恢复；不做多人合并 |
| 交付 | A2、A3.7 | B2/B3 | 工具/按钮同结果，范围和哈希可查 |
| 操作状态、事件与恢复 | A1、A3.3/A3.6 | B2/B4 | 会话内有序、可补读、超时取消、真实写盘状态 |
| Skill 更新 | A4 | B3 | 新版/未知/离线/无效清单/缓存/重复提醒 |

需求示例中的 `register_vocabulary_draft` 是草稿语义登记动作，A0 将其明确纳入 `update_brief` 的结构化草稿字段与后续 `register_icons` 流程；不留下示例有名称、实际能力目录没有的悬空操作。`revoke_session` 等安全入口同样纳入操作注册与权限测试，不只画 UI。

## 8. 测试与验收执行方式

### 8.1 TDD 和增量验证

可自动化行为先写失败测试，再做最小实现。每个工作包验证本次变化和依赖回归，不为每次文案调整重跑所有环境。纯文案/静态规范改动做文档与真实界面检查，不机械制造测试。

| 层次 | 覆盖 | 不可替代的证据 |
| --- | --- | --- |
| 单元 | schema、几何、状态、导出、更新比较与缓存 | 数学断言、合成期望值、失败分支 |
| 合同 | 每个操作的输入/输出/错误，两条入口映射 | 自动检查目录/处理函数/schema 完整一致 |
| 存储集成 | 读写、取消、无权限、失败、后写覆盖、格式不兼容 | 实际文件与操作结果对照，不只 mock 返回 success |
| 流程集成 | 新建→确认→生产→审核→导出→重开 | 分步执行记录、内容版本和文件产物 |
| WebMCP-only | Agent 业务仅使用 WebMCP | 不用本地 HTTP、DOM 或截图调用补缺失业务工具 |
| Skill 回退 | 禁用 WebMCP，只按包内规范使用本地入口 | 不逆向 UI，不要求用户手工代填需求 |
| 浏览器视觉 | 页面、控件、焦点、选区、场景、窄屏、中英文 | 真实运行截图/交互记录，不以 CSS 值相同代替对齐验收 |
| 分发/离线 | 新安装、断网、包扫描、版本/下载、双端接续 | 干净安装位置、网络记录、文件清单、哈希 |

### 8.2 必测的简单覆盖语义

两个客户端都读取 A 版；客户端一保存 B；客户端二仍以 A 为来源保存 C。目标磁盘最终必须是 C，第二次不得收到项目占用或版本冲突拒绝。刷新客户端一后能读到 C。C 覆盖 B 是已确认行为，不记录为需要修复的“丢失更新”。

测试不扩展为全局顺序或多文件事务测试平台。文件 API 自己失败时必须报错，不能把失败当成“后写获胜”。

### 8.3 规模和视觉质量

- 合成数据规模：20 项目、每项目 5 方案；选定压力方案生成 600 图标、每图标最多 4 个登记变体。另验证全库索引不触发所有几何编译。
- 默认页 48、工具最大页 100；记录未打开项目的编译调用数应为零。
- 在明确设备和缓存条件下测查询/分页 p95 ≤ 300ms、缓存详情切换 ≤ 200ms、长任务 1 秒内返回 operationId。不把示例只有两张图标当作性能通过。
- 保留黑白灰、统一控件、标题对齐、方案导航、图标平铺到详情、固定场景、没有常驻“已保存”及没有评价文案。
- 真实验收至少覆盖桌面宽屏、窄窗口、键盘操作和中英文；浏览器/宿主/OS 版本写入记录。

### 8.4 未来工程测试入口

A0 建立并在 README 固定 `test:unit`、`test:contract`、`test:integration`、`test:e2e`、`build`、`pack:skill`、`build:web`、`check:package` 这些非 watch 入口。这里是建设任务，不是现在可以直接运行的命令清单。

现有 `work/test_*.mjs` 和 `work/test_*.py` 可提供回归语义；旧合同测试不强制继续全绿。保留数学与 UI 行为要求，删除仅用于证明旧别名、旧锁和旧版本拒绝机制的断言，并用新需求测试替换。

## 9. 风险与处理顺序

| 风险 | 何时验证 | 处理原则 |
| --- | --- | --- |
| 曲线/布尔/描边几何在共享实现中不一致 | A1 最先验证 | 先合成数学用例；不双引擎兜底、不删能力；若确实无法满足，报告具体阻碍而非自行降级需求 |
| 新旧项目身份混用 | A0/A2 | 新 schema、新操作版本、新数据，拒绝旧格式，不造迁移层 |
| 旧并发保护被顺手复制 | A0/A1/A3 | 操作与存储负向测试，确保不要求基础版本、不占项目 |
| 大库分页只是 UI 装饰 | A3.8 | 检查数据枚举/编译调用量，再做计时 |
| 本地回退只有生成没有完整维护 | A2/A4 | 单独禁用 WebMCP 验收全部流程 |
| 浏览器目录授权不能静默完成 | B1 | 清楚展示系统权限动作、状态可读；不隐藏保存到缓存 |
| 网页误判 Skill 已安装 | B3 | 区分下载、安装、加载及其证据，不构造万能安装 API |
| 更新发布源或域名还没有配置 | A4/B3 | 先用合成源测试，正式发布时注入确切 URL；未连真实源就不声称可在线检查 |
| 打包混入真实数据 | A4/B3 | 白名单构建与包内容扫描，不从整个工作目录打 ZIP |
| 本地/在线版本不一致 | B4 | 核对格式和运行版本，拒绝不支持的格式，不迁移 |

实际域名、发布账号、Windows 验收设备和目标宿主是交付环境资源，不是重新待定的产品需求。对应阶段到达时验证可用性；缺少资源则明确标记该项未验收，不以 mock 代替，不阻碍前面的可执行工作包。

## 10. 完成判定、进度与交付

### 10.1 每个工作包的完成定义

1. 范围内功能实现，关联操作在两条路径中可用。
2. 新失败测试已通过，相关回归通过；没有用跳过测试掩盖未完成能力。
3. 涉及 UI 的工作已检查真实运行界面；所看版本与构建来源可确认。
4. Skill 说明、schema 与运行行为保持一致，不遗留误导 Agent 的旧字段和流程。
5. 记录证据路径、剩余具体问题和实际交付状态。

状态统一为：未开始 → 进行中 → 验证中 → 完成；依赖外部环境时记“受阻 + 缺少什么”。已确认需求不等于计划已经执行，测试通过不等于安装或发布完成。

### 10.2 进度账本

| 工作包 | 状态 | 完成证据 |
| --- | --- | --- |
| A0 | 完成 | 11 文档、22 能力域、96 严格可执行合同；消费者解析与未知字段/旧别名拒绝测试通过 |
| A1 | 完成（本机） | Node 真磁盘、完整替换、隔离配置、回环服务、动态端口、构建身份复用；同几何在 Node/真实浏览器 Worker 输出一致 |
| A2 | 完成（本机） | 原生 WebMCP-only 与无 WebMCP HTTP 两条流程均从空库完成确认/生产/查看/下载；真实停服重开读取相同项目 |
| A3 | 完成（本机） | 维护、选区、固定场景、审核失效、源/SVG 导出、任务暂停续跑；600 × 4 磁盘分页及真实页面渲染/切换测量通过 |
| A4 | 验证中；Windows 实机待验收 | A4-01、A4-02 已修复；Codex 内置浏览器实际只枚举 5 个 WebMCP 网关，并通过网关读取真实 HETAO 项目、规则、图元和三项预览。Windows 实机仍缺证据 |
| B1 | 进行中：存储与目录会话 | 文本/JSON/扫描、目录选择、显式重授权、句柄持久化与目录会话；6 项单测与 2 项真实 Chromium 测试通过。真实用户目录授权、撤权、重连界面尚未验收 |
| B2 | 预览已接通 | 共享项目/图标视图、Worker、5 网关、生产和 SVG 下载、刷新重连通过真实 Chromium；大库主线程与完整能力矩阵仍待验收 |
| B3 | 预览已接通 | 完整包、哈希、宿主指令和 acknowledge_skill；在线下载可用。自动更新源与新用户跨宿主安装矩阵仍待验收 |
| B4 | 预览已部署 | boplet.app HTTPS、原生网关、静态资产与包哈希线上通过；外部目录在线/本地交接和撤权矩阵未完 |

实施中将证据放在工程的本地 QA 输出目录，本文件只链接，不重复粘贴大日志。

### B1 初始增量（2026-09-12）

- 新增 `src/storage/browser.js`，复用路径校验、严格 JSON 和错误合同；完整关闭文件流后才返回保存成功，不添加应用锁、CAS 或隐藏存储回退。
- `chooseDirectory` 由用户手势触发目录选择；`authorizeDirectory` 由明确的重连动作触发重新授权。普通读写只检查权限，不自行弹出授权。
- 验证命令：`node --test tests/unit/browser-storage.test.js tests/e2e/browser-storage.test.js`，5 项通过。覆盖覆盖写入、重开读取、取消、撤权、重授权和保存失败。
- Chromium E2E 使用 OPFS **仅作为隔离测试夹具**，验证真实文件流、页面重载读取，以及与 Node 适配对同一合成输入的结果一致；不是用户所选磁盘目录验收，也不作为产品存储回退。
- 尚未完成：用户目录选择界面及真实权限验收、句柄持久化与清站点数据后的重连、并发保存语义验收、B2 Worker/静态站点装配。未替换已安装 Skill、现有预览或真实图标项目；未部署 `boplet.app`。

后续增量：新增 `directory-memory.js`，IndexedDB 只记录目录句柄；新增 `directory-session.js`，恢复时仅检查权限、取消切换保留原连接、断开只遗忘引用、记忆失败明确返回 `remembered: false`（页面必须提示下次重新选择，不能声称已记住新目录）。真实 Chromium 已验证句柄跨刷新读取、遗忘后文件仍存在；仍使用隔离 OPFS 夹具，不代表外部目录权限验收。定向验证共 8 项通过（`browser-storage`、`directory-session` 单测及 `browser-storage`、`directory-memory` E2E）。目录界面、清站点数据后重选外部目录、并发保存和 B2 静态入口仍待完成。

### B2 Worker 初始增量（2026-09-12）

- B1 存储与目录会话已本地提交 `7334fa6`；未推送，其他既有工作区改动未纳入该提交。
- 新增 `src/web/worker.js`，装配共享 createStudio 和 BrowserStorage，不复制操作合同，不加入本地 HTTP 依赖。一个 Worker 对应一个目录会话，重复初始化拒绝，切换目录须由页面结束旧 Worker。
- `node --test tests/e2e/browser-runtime.test.js` 通过：真实 Chromium 加载静态 HTML/Worker，仅请求这两项静态资源；会话报告 browser_worker，工作流停在 skill_required，合法的项目创建输入被加载门槛拒绝。
- 这只证明运行入口与门槛，不是网页交付。页面连接、目录权限界面、Skill 就绪接入、完整生产/导出、在线构建和部署仍待完成。本增量未改真实数据或常驻预览。

启动页增量：`npm run build:web` 生成 `dist/web`，复用现有 HTML、品牌和 CSS，单独入口装配 Worker 与原生 WebMCP 网关。支持复制宿主指令、剪贴板失败的只读替代、目录选择/重授权/遗忘控件；无原生 WebMCP 时禁用目录入口并引导本地 Skill，不请求 localhost。目录目前仅验证授权，未传给 Worker，不声称已连接项目库。`web-start` 和 `browser-runtime` E2E 两项通过，真实 Chromium 截图 `.qa/web-start-1280.png`、`.qa/web-start-390.png` 已检查，未发现横向溢出；未完成真实外部目录权限验收。测试浏览器及临时构建已由钩子关闭/清理，无新常驻预览，现有预览和真实数据未动。后续继续 Skill 就绪确认及原业务视图接入；未部署或推送。

### B2 宿主就绪与 Worker 落盘增量（2026-09-12）

- 共享合同新增 `acknowledge_skill`（当前 97 项操作），要求完整 Skill 加载声明、版本、HTML 同意及证据文本；必须有 HTML 会话。来源标记 `host_attestation`，不声称检测到安装或验证了人类身份。页面不得替 Agent 自动声明就绪。尚未接入发行版本/哈希核对，不代表 B3 分发验收完成。
- Worker 支持首次授权句柄绑定，绑定后不允许同 Worker 换目录；只调用共享 BrowserStorage 和业务核心。真实 Chromium E2E 验证未就绪拒绝、声明就绪、绑定句柄、建立 Library、创建 Project，以及直接读取已写文件与操作结果一致。OPFS 仍仅为隔离测试夹具。
- `npm test` 80 项通过，`browser-runtime` E2E 通过。页面选择句柄尚未转交 Worker，原项目视图和完整生产/导出仍未接入，不能称完整在线运行。未改变真实项目、现有预览、安装或域名配置。

### 网页预览发布（2026-09-12）

- 用户授权使用 Cloudflare CLI 部署；确认现有 OAuth 登录及 boplet.app active zone，同账号下新建独立 `boplet` 静态 Worker，绑定自定义域名；没有改其他站点。线上地址 https://boplet.app，备用 https://boplet.zhaowork74.workers.dev。
- 最终部署版本 `b191683e-8323-4ea7-9510-c0fb43eb56a4`；上一预览版本 `a47627bd-117b-4c58-a2bf-d2047d45b59a` 可用于程序回滚。
- 网页目录会话绑定 Worker 后复用原 app.js 业务视图，在线构建消除本地 bootstrap/operation HTTP 分支。原生网关在页面切换时保持注册，避免正在返回的回执被注销中断。不以页面点击伪造 Skill 加载，不自动创建库；空库仍由获授权 Agent 显式 connect_library(create:true)。
- `npm test` 80 项通过；本地原生流程回归通过；新增静态网页 E2E 从完整宿主声明、选目录、需求确认、生产、编译、图标详情到 SVG 按钮下载、刷新重连通过。目录选择器仅在测试中替换为隔离 OPFS 句柄，原生 WebMCP/Worker/文件 API 未伪造；不等同真实用户外部目录撤权验收。
- `scripts/release-web.js` 生成发布白名单资产与完整 Skill ZIP，先经 inspectPackage 检查再发布；线上 release.json 和 ZIP 哈希一致。全站 no-store 避免 HTML/核心跨版本缓存；CSP 限同源连接，Ajv 现有编译器需要 unsafe-eval，沿用视图需要 inline style，暂未宣称最严格 CSP。
- 线上 HTTPS 返回 200；真实 Chromium 枚举 5 个网关、无页面异常、390px 无横向溢出。截图 `.qa/boplet-live-desktop.png`、`.qa/boplet-live-mobile.png`；图标详情证据 `.qa/online-structure.png`。测试专属浏览器和服务器已关闭，未改真实图标库或既有 55822 预览，未替换全局安装，未提交/推送本轮代码。
- 本次交付为可访问预览版，不是 B1–B4 全部验收：外部目录真实权限/撤权、双端同目录交接、并发保存和自动更新仍保留缺口。后续发布使用 `node scripts/release-web.js` 后 `wrangler deploy`；回滚使用 Cloudflare 已记录版本，程序回滚不迁移用户文件。

### 10.2.1 A0 基础增量（2026-09-08）

以下是首次基础提交的历史记录，已被后续 §10.2.2 增量覆盖；其中“尚未实现”只描述当时。

- A0.1：ESM JavaScript 工程、锁定依赖、单次 unit/contract/integration 测试及合同构建已建立。当前 Node 22.23.1、npm 10.9.8、macOS；Windows 未验。完整 E2E/打包/网页命令仍明确失败，不以空测试充数。
- A0.2：Library/Project/Brief/Vocabulary/Scheme/Rules/Matrix/Task/Batch/Review/Delivery 共 11 类文档 schema；独立身份、严格嵌套、版本及状态维度有测试。生产门禁、规则影响等业务行为仍须随实现继续验证，不把 schema 当成完整业务实现。
- A0.3：单一目录覆盖 22 个能力域、96 个操作，18 个已有输入/输出 schema 和核心处理函数；剩余 78 个明确标为 `NOT_IMPLEMENTED`、schema 为 null。尚未完成所有合同，因此 A0 不标完成；尚无 HTTP 或真实 WebMCP 注册。
- A0.4：MemoryStorage 和空库、零方案、同名多项目、多方案、尺寸/样式/轻重的合成夹具已建立。只验证核心与测试适配器，不证明文件落盘或整套工作流可用。
- A0.5：旧格式拒绝、不使用旧 projectKey、旧 sourceRevision 不拦截、双会话合法覆盖、非法路径及嵌套字段等回归通过。真实 OS 文件权限与安全替换尚待 A1。
- 测试遵循先失败后实现。另覆盖同 requestId 并发重试、JSON 键顺序、回执过期、返回对象隔离、存储失败、未知目录不覆盖、撤销仅影响当前会话。

当前验证入口：`npm test`（32/32），`npm run build`（只生成合同包），`npm audit --omit=dev`（本次报告 0 漏洞），`git diff --check`。本地简要证据：[A0 验证记录](../.qa/A0-foundation-2026-09-08.md)；该文件为本地 QA 产物，不入 Git，其他 checkout 可按上述命令重现。

下一步先完成剩余操作的严格合同和具体验证用例，尤其 brief 确认、规则、登记、几何与任务结果，再按 A1 优先验证共享几何与真实 Node Storage。未改当前预览、未做视觉验收、未替换已安装 Skill、未部署，且本轮没有需要保留或清理的产品服务进程。

### 10.2.2 A1–A4 候选增量（2026-09-08）

代码、Skill 规范和容器全部在本仓库实现，没有启动子 Agent，没有复制真实图标数据。验证使用合成确认，不作为真实用户设计接受记录。

- **共享生产**：Paper.js core（MIT）实现显式路径/控制柄、曲线边界、圆角、四类布尔、组、组件和仿射变换；实例拆分及拆组保持变换。矩阵是源，SVG 是产物。任务登记和当前确认版本为生产门禁；校验/编译与用户接受独立。
- **状态与恢复**：上下文精确到项目/方案/图标/变体/层/节点/柄；场景清空隐藏选区；显示设置工具可读回。受理、权限等待、失败和完成分别回报，批量逐项结果不能当全部成功。需求发布中途失败保留可读取的旧引用，任务/checkpoint 可在真实停服后继续。
- **维护**：独立方案复制/归档恢复、语义 tags、登记变体/批次、层与节点编辑、固定用途/尺寸对比/反色、用户原文反馈、版本审核及几何历史恢复。源快照包含项目、需求、语义、方案、规则、矩阵、组件与用途，不是旧版本迁移包。
- **容器**：沿用 DESIGN.md 的黑白灰、标题行对齐和 list→detail；没有顶部两模式 Tab、卡片选中态、重复方案标题、手工表单/几何编辑或主观评价。尺寸只属于结构，场景从全图标绑定生成。节点具备独立 24px 命中区和键盘选择。
- **更新**：固定来源校验、24 小时成功缓存（独立配置跨会话）、强制重查、同版本提醒去重、未知/离线/无效清单明确状态。候选包未配置真实发布源，返回 unknown；未自动安装或迁移。
- **打包**：SKILL.md、四篇操作/方法规范、单一 contracts.json、HTML/CSS/JS、本地 runtime、版本/文件哈希清单及依赖许可证共 19 文件。独立包运行无 npm/CDN 依赖，宿主须提供 Node 22.x。候选 ZIP 约 265 KiB，当前 SHA-256 见 outputs/icon-studio-next/sha256.txt；输出不入 Git。

当前证据与复现入口：

| 验证 | 结果与证据 |
| --- | --- |
| 核心/合同/集成 | npm test：58 项通过；覆盖写入失败、旧副本合法后保存、严格合同、长内容确认、多文件失败恢复等 |
| 两条完整路径 | tests/e2e/local.test.js：真实 Chrome 默认无 WebMCP 与开启实验能力的原生 document.modelContext 各自完成生产、选区、反馈、审核失效、暂停、停服重开、续跑及导出；业务不通过 DOM 代填 |
| 共享浏览器引擎 | tests/e2e/worker.test.js：真实 Worker 与 Node 的曲线、圆角、布尔结果一致；不是浏览器目录授权或线上部署验收 |
| 大库磁盘 | tests/integration/scale.test.js：20 项目 × 5 方案，压力方案 600 图标 × 4 变体，当前页 48 次矩阵读取，无全库编译；Apple M4 / Darwin 25.5.0 / Node 22.23.1 热缓存查询 p95 约 15ms，最新值在 [.qa/scale.json](../.qa/scale.json) |
| 大库真实页面 | tests/e2e/scale.test.js：1903×1320 Chrome 152，仅 48 张卡片、标题行对齐，最终回归缓存详情切换 p95 约 23ms，首屏约 375ms；见 [.qa/browser-scale.json](../.qa/browser-scale.json) |
| 实际视觉 | 桌面结构/场景、390px、中英文、键盘节点、600 卡片分页截图在 .impeccable/review/；本 Agent 复核，未伪称独立评审。自动检测器缺少解析器而降级，零命中不表示完整可访问性审计通过 |
| 独立包 | tests/integration/package.test.js：仓库之外临时安装目录，捆绑引擎实际编译并导出；启动/重开/移除程序后 library 保留；pack:skill/check:package 通过 |
| Skill 规范/依赖 | skill-creator quick_validate 通过；运行依赖 npm audit --omit=dev 本次 0 漏洞；未执行全局 Skill 安装 |

**A4 尚未关闭的交付门槛**：Windows 实机证据缺失，不能用 macOS 路径测试或命令示例替代。本会话没有可直接执行测试的 Windows 环境，未为此新建或转移用户任务。真实官方更新源属于 B3 发布资源，尚未接通；合成源测试不表示在线提醒已上线。阶段一仍是候选，不宣称完整跨平台发布。

测试专属浏览器、服务和临时安装由测试钩子关闭/清理；没有保留新版常驻预览、没有终止旧服务或其他项目进程。当前只交付代码与候选包；安装、发布、推送均未执行。

### 10.2.3 A4 交付 ZIP 验收（2026-09-08）

对象为提交 `976e43e` 对应的 `0.1.0-dev.1` 候选 ZIP，SHA-256：`c1420e7525e887edfa850d90f786e634a2e23a384c04b92e1433f7d5fede0ac2`。本轮只验证、记录，不修复产品代码、不重新打包、不全局安装。依据 skill-creator 校验规范检查说明、引用和运行行为；未启动子 Agent，不声称独立盲测通过。

**macOS 通过项**：

1. 从实际 ZIP 解压至仓库外含中文、空格的隔离安装目录；19 文件白名单及每文件哈希通过，Skill frontmatter 校验通过。
2. 未提供加载声明时启动拒绝，未创建数据目录；完整读包规范后提供宿主加载声明并启动。该声明不是 Agent 自动发现或加密验证。
3. 同构建再次调用复用既有 PID；短启动器退出不关闭运行服务。
4. macOS sandbox-exec 限制产品进程只可访问回环网络；先用外网连接返回 EPERM 验证限制，再执行合成项目、需求确认、规则、几何生产、编译及 SVG 导出。未配正式更新源时返回 unknown，没有假报最新版。
5. 解压包的实际 HTML 容器正常显示编译图标、结构和下载入口，无页面异常或外部资源请求。浏览器只放行该回环来源；截图已人工查看。合成几何只用于运行验收，不当成图标设计质量或真实用户接受证据。
6. 停服后不传目录重新启动，沿用独立配置中的原目录，项目 ID 和导出内容一致。
7. 卸载隔离程序后独立数据文件不变。随后清理本次合成数据与临时安装，精确关闭本次服务和浏览器；未修改真实项目、旧预览或全局 Skill。

**不通过项 A4-01（P2）**：预发布版本更新漏报。当前 `0.1.0-dev.1`，合成合法发布清单 `0.1.0-dev.2`，期望 update_available，实际 up_to_date；正式版本递增及候选转正式对照用例通过。根因在 `src/core/updates.js` 的 `newer`：去掉连字符后的预发布标识后只比较三个主版本数字，不比较预发布序号；交付包 runtime 中包含相同逻辑。尚未修复。关闭条件：补预发布版本顺序的失败测试、修复并回归缓存/提醒逻辑、重建候选 ZIP 后复验。

证据：[本地验收脚本](../.qa/acceptance-a4.mjs)、[逐项结果](../.qa/A4-acceptance.json)、[解压包离线页面](../.qa/A4-installed-offline.png)。QA 文件为本机产物，不进入分发包或 Git。此次新增定向验收没有机械重跑上一轮已经通过的 58 项及 4 项测试。

**结论：有条件通过 macOS 核心运行流程，不签署完整 A4 通过。** 除 A4-01 外，Windows 实机和目标宿主加载证据仍缺；当前工具没有可直接执行 Windows 命令的通道，未擅自创建远程任务。正式更新源尚未接通，仍属于 B3 发布资源，不算在线提醒已上线。下一步先修复 A4-01，再补 Windows/目标宿主验收；不以纯网页阶段掩盖 Skill 未关闭项。

### 10.2.4 A4-01 修复复验（2026-09-08）

用户授权修复后，按先失败测试、再实现、再复验执行。A4-01 已关闭；§10.2.3 保留首次验收时的失败证据，不作为当前状态。

- 版本比较依照 [SemVer 2.0.0 第 9–11 条](https://semver.org/#spec-item-11)：逐段比较预发布标识，数字按数值、非数字按 ASCII 排序，正式版高于对应预发布版，构建元数据不影响优先级。无新增依赖。
- 新增三组行为测试覆盖 dev.1/dev.2/dev.10、alpha/beta/rc、相等/倒退/候选转正式、大数字、构建元数据、非法清单和未知当前版本；同时验证缓存跨会话结果与提醒去重。先观察三组失败，修复后更新测试 5/5，全量核心/合同/集成 61/61 通过。
- 重建未发布候选（仍为 0.1.0-dev.1，以构建哈希区分，不代表同版本正式发布）。新 ZIP：271196 字节、19 文件，SHA-256 `92e03971cffdf3ab7551f92757a4daed177720024fb71ddf6ec381732c617d56`。旧候选保存在本机 `.qa/candidate-before-A4-01-fix/`，可恢复。
- 新 ZIP 再次解压至中文/空格隔离路径，macOS 禁止产品进程访问外网条件下完成生产、导出、复用、重开与卸载保留数据。检查实际捆绑更新模块（合成 fetcher、同合同 schema 校验）确认 dev.1→dev.2、候选→正式及正式递增均为 update_available；不以此宣称真实更新源在线可用。
- [复验脚本](../.qa/acceptance-a4-fix.mjs)与[结果](../.qa/A4-fix-acceptance.json)为本机 QA 产物；包检查通过。本次没有 UI 实现改动，不重复全套视觉/E2E；解压包页面已在复验中正常加载。测试进程及临时数据已清理。

剩余门槛不变：Windows 实机/目标宿主加载证据、B3 正式更新源。未替换全局 Skill、未发布、未推送，也未进入网页部署。

### 10.2.5 授权本地安装与 HETAO Station 真实使用（2026-09-08）

用户明确授权安装并继续以 HETAO Station 测试 Skill，随后确认使用新的独立数据目录。该授权覆盖本次全局 Skill 替换，不覆盖旧数据迁移、HETAO App 资源接入、自动用户确认或发布。

- 将 §10.2.4 已验收 ZIP 解压、校验 19 文件和哈希后安装到本机 Codex Skill 目录；原版本移入 `<USER_HOME>/.codex/skill-backups/make-product-icons-20260908-zGaa9Q/previous`，可回退。已读取安装后的 SKILL.md，并通过元数据校验；当前任务手动加载，新一轮可自动发现。
- 从全局安装的 runtime 启动新版服务（不是仓库源码或旧 62770 预览），用户授权的新库与全局配置分别存储。保留新版服务和浏览器页面供用户继续审核，旧服务未终止。
- **A4-02（P1，未修复）**：Codex 内置浏览器能显示页面及 WebMCP 工具通知，但实际取得/调用工具时报告 `site's WebMCP configuration exceeds supported limits` 并禁用该页面 WebMCP。尚未确定是工具数量、schema 总体积还是嵌套限制，不能猜一个阈值直接裁剪能力。此前 Chrome 直接调用 document.modelContext 的 E2E 只验证浏览器原生接口，没有覆盖 Codex 工具通道限制，不能作为该宿主可用的证明。
- 按已确认回退规则改用同一已安装服务的 HTTP 操作合同，完成空库读取、真实项目创建、需求来源整理、草稿写入、校验和 prepare_confirmation。浏览器观察确认六项需求自动同步。当前停在真实需求确认，尚未生成几何、记录用户接受或导出；不能把这次前半段称为端到端全部完成。
- 此轮测试未修改产品实现。后续先由用户确认需求继续真实设计流程；A4-02 需要单独定位并修复注册装配，保持 96 项业务能力和两端同合同，不以删除能力解决宿主限制。

### 10.2.6 首次需求确认页布局修正（2026-09-08）

- 根据用户实际页面批注，将无方案项目改为居中需求确认页；移除空方案侧栏和“暂无方案”重复提示，返回导航只保留“返回项目库”。顶部为“确认需求 / 生成方案 / 微调细节”的只读步骤，使用用户指定引导文案。需求正文仍只读，不新增网页表单。
- 进度取实际 brief.status；确认后切到“生成方案”，创建方案后恢复既有导航，不改变生产门槛或已确认的图标列表/详情结构。规范已同步 DESIGN.md。
- 先在现有 E2E 中加入断言并观察侧栏隐藏测试失败，再实现；本地 HTTP 与浏览器原生 WebMCP 两条完整流程均通过。新增覆盖无方案、需求确认、方案创建后导航恢复、非交互步骤、无手工输入、桌面居中、390px 与中英文无横向溢出。原生接口测试仍不代表 A4-02 的 Codex 工具通道已修复。
- 重建并检查 19 文件候选包，ZIP SHA-256 为 `87d31c48089351c42242de38632967da92cad2a799c17a33584d3d13f8a42a2c`；安装目录整体备份后更新，保留旧候选和旧安装以便恢复。仅重启本任务拥有的新版服务，旧 62770 服务未动。
- 已在当前 Codex 浏览器查看实际安装包的 HETAO Station 需求页，并检查桌面与 390px 实际排版，恢复默认视口后保留页面。新服务为 `http://127.0.0.1:49223`；原 61875 实例已结束。实际需求内容与待确认状态未更改，没有生成方案或代替用户确认。
- layout / impeccable 检查沿用现有中性色和间距，重点修正阅读顺序与窄屏重排；其旧 spatial-design 引用不存在，采用现有 layout playbook。检测器因缺少解析依赖使用降级扫描，不能将无匹配项当完整质量证明；以运行页面和 E2E 为验收依据。本次未修改核心/合同，不机械重复其全量测试。A4-02、Windows 实机与正式更新源门槛不变。

后续批注修正：删除进度条可见的“步骤”前缀（仅保留无障碍列表名称），所有屏幕宽度统一为字段名一行、内容从下一行开始并自然换行。新增失败测试后修复，本地 HTTP 完整 E2E 通过，包检查及已安装页面桌面/窄屏复核通过。此次不改业务合同，未重复原生通道全流程测试。候选 ZIP SHA-256：`a73dd643ac9df8c59401bcda3983c946adf34b5aadc9824b49cb801249ed2ac4`；备份安装后重启本任务服务，当前预览为 `http://127.0.0.1:50398`，49223 实例已关闭，需求数据保持不变。

### 10.2.7 需求目的与风格偏好分离（2026-09-09）

- 用户确认“目标”实际混合了风格偏好与造型建议，改为 `stylePreferences` / 风格偏好；`purpose` 说明为什么设计、解决什么问题。Agent 的具体造型建议留待方案探索，用户明确指定的造型则作为有来源的约束保留。
- 同步文档 schema、WebMCP/HTTP 单一输入输出合同、缺项校验、确认摘要、页面中英文与 Skill 规范。校验字段直接从 schema 取得，避免另一份列表漂移；拒绝旧 `goals`，不增加兼容字段或自动迁移。当前仍是未发布候选，以构建哈希区分。
- 新增先失败后通过的行为测试，覆盖 stylePreferences 缺项、保存、确认及拒绝 goals 后原确认不变；核心/合同/集成 62/62，通过本地 HTTP 和浏览器原生 WebMCP 两条完整 E2E。Skill 元数据校验、19 文件候选包检查通过；不表示 Codex 宿主 A4-02 已解决。
- 经用户授权用正常操作合同修改当前 HETAO 草稿：原字段删除后保存历史，新字段只保留“安静、可靠、克制。”；具体壳/核/负空间不再约束需求，“不绑定某一项功能”归入约束而非丢弃。未改其他字段或替用户确认；旧历史仅封存，不转换旧项目。
- 新候选 ZIP SHA-256：`edf4c85781d9fb81a5188d695c4d988b08d7a16ce7ccca29151fd1daf2f4a8e5`。备份并更新已安装 Skill，只重启本任务服务，当前地址 `http://127.0.0.1:51599`；50398 实例已结束，新实例保留供审核。真实页面确认显示新字段与草稿内容。未发布、未推送、未生成图标。

### 10.2.8 HTML 中介启动同意（2026-09-09）

- 用户新增明确启动门槛：先说明 Skill 必须通过本地或在线 HTML 页面运行，询问并等待明确同意；拒绝或未答复不启动、不复用，也不绕过页面直接生成文件。与目录授权、需求确认及 WebMCP 支持分开，同一连续任务内有效同意可用于技术重启。
- Skill 入口、运行规范、两条流程和产品需求已同步。启动入口和 launcher 均校验同意声明/答复引用；检查发生在目录创建和实例复用之前。HTML 三项资产缺失也拒绝启动。同意声明是宿主如实转述，不是加密证明，不缓存成永久同意。
- 产品启动器强制 requireUI：初始与新增会话未 bootstrap 连接时，get_workflow 返回 html_required，项目设计操作拒绝。内部核心的无界面实例仍供单元测试使用，不提供产品启动绕过开关。连接记录不代表持续可见性，未加入复杂的浏览器存活监控；规范要求页面已知关闭/失效或用户撤回时停止新设计操作，不能假称能自动检测所有浏览器关闭。
- 先观察缺少门槛的失败测试，再实现。全量核心/合同/集成 64/64 通过；真实浏览器分别以原生 WebMCP 与 HTTP 完成两条带 requireUI 的 E2E。独立包测试覆盖未同意/拒绝不创建数据及配置、同意后启动、实际打开打包 HTML 后完整生产与重开。Skill 元数据和候选白名单检查通过；没有新增页面视觉布局。
- 当前仅完成仓库与候选包，不替用户回答本次 HETAO 的 HTML 同意问题；未更新全局安装、未重启既有 51599 服务、未修改真实数据。已在对话询问，等待用户答复后再安装并按新规则启动。旧预览继续保留，不误报为已运行新的启动门槛。A4-02、Windows 与在线入口仍未验收/部署。

后续提示文案修正：按用户批注补充“如果需求信息没问题，请回复‘需求已确认’”，同步英文、界面规范与 E2E 预期；该文案不是自动确认指令，本次未确认实际需求。本地 HTTP 完整 E2E 通过，已检查合成项目在桌面与 390px 窄屏的真实浏览器截图，文字完整且无横向溢出。重建候选供后续安装；当前真实预览仍未替换，继续等待本次 HTML 使用同意。

后续安装与运行：用户在本次对话明确回复“允许”后，已备份旧安装到 `<USER_HOME>/.codex/skill-backups/make-product-icons-html-WEhPvw/previous`，安装已校验候选（ZIP SHA-256 `b8fbc94800c229353649e02548088544f2f36822fc126fd5d8430a824b228d51`）。安装目录与候选逐文件一致，Skill 元数据校验通过。按真实答复引用启动，当前服务为 `http://127.0.0.1:61846`，运行 buildId 为 `sha256-7384eb71c2fdf24090796c159a3dd8e2bc6798372fca67ce6ff69a766a4c3114`。已复用原浏览器标签打开并观察 HETAO 真实需求页，新提示完整显示；get_workflow 返回 brief_required，未代替用户确认需求。数据目录前后均为 9 文件，完整内容哈希一致。仅关闭已核实归属的旧 51599 服务及其启动器，新服务保留供审核；无发布、推送或旧数据迁移。上述等待 HTML 同意事项已解除。

后续真实流程测试：用户明确回复“需求已确认”后，核对页面、需求 revision 和 prepare_confirmation 摘要一致，通过已安装容器的同合同 HTTP 回退完成 confirm_brief，回读为 confirmed 且持久化成功。已创建三个独立方案草案并保存各自规则提案，实际页面显示三个方案入口；规则尚未获用户确认，未登记生产任务、生成几何、编译或记录用户接受。现有服务继续保留，A4-02 原生宿主通道问题未因此视为关闭。本轮仅使用真实业务合同，不修改产品实现或重复合成测试。

### 10.2.9 需求确认后直接出图与方案顺序（2026-09-09）

- 用户明确纠正二次规则确认：需求确认后 Agent 直接完成首轮图标，不能停在空方案框架再次询问方向、网格、描边和圆角。已同步 Skill、流程/数据/设计方法、产品需求及界面规范；用户确认需求和看图后的接受仍独立保留。此决策替代前述历史记录中的“等待规则确认”。
- 合同层移除 propose_design_rules / confirm_design_rules，合并为 set_design_rules；prepare_confirmation 仅接收项目需求。规则为 draft/ready/superseded，不再携带用户 confirmation。保存合法规则直接设置可生产版本；需求未确认、归档目标、非法参数仍拒绝，规则更新后旧任务不得继续生产。不增加旧规则映射或迁移。
- 列表根因为沿用存储目录顺序，不是视觉反转。list_schemes 统一按名称自然升序，再以创建时间与身份稳定补序；先排序后分页。覆盖 A/B/C、数字编号和修改后顺序保持，不修改真实方案名称来规避问题。
- 先观察两项回归测试失败再修复；全量核心/合同/集成 66/66，本地 HTTP 与原生 WebMCP 两条完整 E2E 通过，覆盖无规则二次确认直接编译、分页排序及实际侧栏顺序。当前合同为 22 域、95 操作；Skill 校验、19 文件包检查通过。
- 候选 ZIP SHA-256 `0d8e5935013c4188c528bfd81a1e58c84f24f25027a15f765b4f27215e7187ce`。沿用已授权安装及本次连续任务 HTML 同意，旧安装备份至 `<USER_HOME>/.codex/skill-backups/make-product-icons-direct-B1l3tB/previous`，更新后逐文件校验一致。关闭已核实的 61846 实例；当前已安装服务为 `http://127.0.0.1:64237`，buildId `sha256-2ff97812d88615e0fb3948cbb1332111a4a8d428ec901a61393fee7c420b6f52`，保留供审核。
- 用同合同 HTTP 回退继续真实 HETAO 项目：保留已确认需求与三个方案身份，Agent 重新制定规则，登记应用/状态栏两个独立语义，每方案应用 16/64 px、状态栏 18 px，共 9 变体。全部完成任务、几何校验和编译，回读 9 个任务均 succeeded；绑定应用/状态栏固定场景，未伪造用户接受、未导出或接入 HETAO App。
- 实际已安装页面显示 A/B/C 与图标，应用场景已核查；另从持久矩阵预览绘制 9 变体校验图，检查实际大小及黑白反色。独占截图浏览器已关闭，预览服务保留。没有提交、推送或部署；A4-02、Windows 实机和在线发布源门槛仍未关闭。

### 10.2.10 图层检查与内容对齐（2026-09-09）

- 根因：选区合同有效，但前端只为 path 绘制节点，rect / boolean 等没有画布反馈；属性只显示身份、类型、绘制方式。现在 get_layer 返回包含祖先变换的独立 SVG 与边界，布尔子图层可检查原始操作数；界面显示黑白虚线轮廓、父子缩进和完整几何属性，不修改生产图标。点击重绘后保留图层/节点焦点，避免 SVG 原生焦点圈随视图放大遮挡图形。
- 按 layout 的内容分区原则：导航、标题、操作及正文左对齐；画布、实际尺寸正反色对比居中；固定场景内容栏限宽居中。视觉验收发现窗口缩窄后画布未重算的问题，一并修复。没有扩展为手工几何编辑器。
- 先取得缺少 previewLayer 与缺少画布选区的失败测试。核心/合同/集成 67/67；全套 4 项 E2E 通过（含 HTTP、原生 WebMCP、600 图标和 Worker），补充窄屏画布重算后定向 HTTP E2E 再次通过。覆盖嵌套变换、布尔操作数、几何不变、尺寸属性、缩进、焦点、节点选择与实际尺寸/场景居中。已看桌面和 390px 截图，并在安装后的真实 HETAO 页面点击“内空”核对轮廓和尺寸、切换场景验收。layout 静态检测因缺少 HTML 解析依赖降级为正则，不作为完整视觉证明。
- 更新本地已授权安装，旧包保存在 `<USER_HOME>/.codex/skill-backups/make-product-icons-layers-lTkFo1/previous`，新包 19 文件检查和安装目录比对通过。ZIP SHA-256 `f03b285fe1f99f1851dad6af8cddc772e1e01a92f8969813cdb89204c810632b`。精确关闭旧 64237 实例及其已退出父进程；新服务 `http://127.0.0.1:51488`、PID 27766、buildId `sha256-2a13ecc48a1ea892364c31ed7f7824bd925b8621e6d0d200108d9f91033a95b3` 保留。真实项目仅切换查看状态，未改需求、几何、任务或审核结论；测试浏览器已关闭。未提交、推送或部署，既有阶段门槛不变。

### 10.2.11 标题模式切换与预览尺寸稳定（2026-09-09）

- 模式切换并入图标标题右侧，窄屏按可用空间换行。遵循 layout 的局部结构调整，不改变内容和已确认的整体界面。
- 实测旧安装版 16→64 px 时，框宽由 1343 变成 1332 px：实际尺寸样本使框高增加 32 px，触发页面滚动条。现按最大有效变体预留样本空间，并稳定滚动条槽位，不缩放实际尺寸样本来规避问题。
- 新增失败 E2E 后实现；HTTP 与原生 WebMCP 两条完整 E2E 通过，覆盖标题右侧关系、切换时预览框宽高一致、窄屏与既有选区/导出流程。桌面和 390px 截图检查通过；静态 layout 扫描仍因解析依赖缺失降级，不作为完整验收依据。真实 HETAO 页面实测 16/64 两态框均为 1332 × 933.4375 px，位置也一致。
- 19 文件包校验和安装比对通过，ZIP SHA-256 `5f1514fce71822d93349b9b265536e217cabc5e7ae8bb2ff444e7b1f779ae291`；旧安装备份 `<USER_HOME>/.codex/skill-backups/make-product-icons-heading-z339Lv/previous`。原 51488 实例精确停止，新安装预览 `http://127.0.0.1:53549`、PID 36120、buildId `sha256-c320e2e28f893f7a11e6f72d5cc2bfc2d1d6b81f80e2651d33221322f9d171fe` 保留。只切换查看状态，未修改真实图标数据；未提交或发布。

### 10.2.12 下拉箭头对齐（2026-09-09）

- 原生 select 箭头不受现有内边距统一控制。按 polish 的局部修正原则统一为 16px SVG 箭头，右侧 12px、垂直居中并预留文字空间；保留原生 select 语义及操作，强制颜色模式回退原生箭头。标签与缩放共用样式，不引入自制下拉菜单。
- 本地 HTTP 全流程 E2E 通过，新增标签筛选及清空后的合同状态回读；桌面、390px 与更新后的真实 HETAO 页面视觉检查通过。19 文件包校验和安装比对通过。旧安装备份 `<USER_HOME>/.codex/skill-backups/make-product-icons-select-3aoNrf/previous`；ZIP SHA-256 `2bd283ba18e31276861849bc27ea0a420d2b30df41f06fd8fb7cf5904003f34e`。
- 精确关闭旧 53549 实例，新预览 `http://127.0.0.1:55793`、PID 42008 保留。没有修改真实图标数据，未自动提交或推送；远端仍待用户提供。

### 10.2.13 精简尺寸切换区（2026-09-09）

- 按用户批注删除“尺寸 / 样式”可见前缀及其孤立样式，只保留已有变体按钮，不改变切换行为。使用 distill 的局部精简原则，不扩展重排。
- 本地 HTTP 完整 E2E、19 文件包检查和安装目录比对通过；真实 HETAO B 方案页面已核查标题消失、按钮完整。旧安装备份至 `<USER_HOME>/.codex/skill-backups/make-product-icons-label-WafI4s/previous`。
- 精确停止旧 55793 服务及其启动器，更新后预览 `http://127.0.0.1:56738`、PID 44945 保留供审核。未修改真实图标内容，未提交或推送。

### 10.2.14 错误提示不挤占布局（2026-09-09）

- 将错误提示改为右下角固定浮层，保留 alert 语义、错误内容和现有清除机制；限宽、长文本换行及高度溢出滚动，不再推移主布局。按 polish 原则仅修正局部缺陷。
- 网络请求中断的失败测试复现内容顶端从 72px 下移至 118px；修复后 HTTP 全流程 E2E 通过，断连前后内容边界完全一致。桌面和 390px 真实浏览器错误态截图检查通过。
- 包检查及安装比对通过；旧安装备份 `<USER_HOME>/.codex/skill-backups/make-product-icons-alert-TWZlap/previous`。精确停止 56738 实例及启动器，新预览 `http://127.0.0.1:57780`、PID 49943 保留，并已打开同一 HETAO 图标列表、确认运行样式生效。未修改真实图标内容、未提交或推送。

### 10.2.15 贯通式侧栏导航（2026-09-09）

- 按用户 Figma 参考取消通栏头部，224px 左栏顶部集中暂定示意图标、项目名、属性和语言；方案位于中部独立滚动，需求和返回项目库沉底。主区从页面顶部开始，详情按需显示右栏。首次需求页不显示空方案导航，仍保留项目身份；窄屏上下重排。同步 DESIGN.md，未引入 Figma 编辑器或额外工具栏。
- 使用 layout Skill，先失败后实现侧栏归属、主区顶端及底部入口断言；HTTP 完整 E2E 通过，桌面及 390px 截图检查通过。静态扫描降级，不替代视觉证据。已安装实际 HETAO 列表和进入详情验证通过，主区 top=0、左侧头部宽223px，右栏仅详情显示。
- 候选 19 文件检查与安装比对通过；备份 `<USER_HOME>/.codex/skill-backups/make-product-icons-sidebar-Lwlt3n/previous`。旧57780实例及启动器精确停止，新预览 `http://127.0.0.1:64226`、PID65049 保留。未修改真实图标内容、未提交或推送。

### 10.2.16 项目信息与方案分组（2026-09-09）

- 按用户进一步批注，顶部只保留项目身份、返回项目库和语言；中部分为项目信息（需求、属性）与方案，不再把需求沉底。属性在侧栏内展开，存储位置与现有运行信息归入其中；首次需求页返回入口统一移至顶部，不重复出现。
- get_storage 同合同新增 location：仅已连接 Node 目录返回适配器的真实路径，未连接或其他适配器返回 null；不写死真实项目目录、不扩大文件读写权限。
- layout 局部重排，先取得顶部入口缺失的失败测试再实现。HTTP 全流程 E2E、核心/合同/集成 67 项通过，覆盖导航归属与真实目录合同回读；桌面和 390px 截图检查通过。实际已安装 HETAO 属性展开验证显示真实目录，收起后保留默认布局。
- 19 文件候选检查及安装比对通过，备份 `<USER_HOME>/.codex/skill-backups/make-product-icons-groups-ElUI4j/previous`。旧64226实例及启动器已精确关闭；新预览 `http://127.0.0.1:52742`、PID81112 保留。未改真实图标内容、未提交或推送。

### 10.2.17 合并项目信息与顶部导航样式（2026-09-09）

- 按用户意见将需求与属性合为单一项目信息页面，侧栏仅一个入口；存储位置、运行信息及刷新/断开按钮在需求正文下直接显示，不折叠。项目库也保留目录操作，无新增表单或业务模式。
- 使用 distill 合并重复入口、layout 明确导航层级；顶部为带边界的返回箭头与语言按钮，下一行为示意图标和项目名。箭头保留名称和悬停提示，键盘焦点沿用既有规范。
- 先取得合并入口缺失失败测试，再实现；HTTP 完整 E2E 通过，覆盖无折叠、真实路径、刷新、断开后路径不再暴露、重新连接与错误不挤占布局。桌面、390px 及实际 HETAO 项目信息页面检查通过。
- 19 文件包检查及安装比对通过；备份 `<USER_HOME>/.codex/skill-backups/make-product-icons-projectinfo-GxpA3i/previous`。旧52742实例及启动器精确停止，新预览 `http://127.0.0.1:54396`、PID88949 保留。未更改真实图标内容、未提交或推送。

### 10.2.18 命名风格限制（2026-09-09）

- 按用户批注在 Skill 工作顺序前加入命名约束：新建或重命名项目、方案、图标不使用中点 `·`，编号和补充词用空格连接。仅约束命名，不替换尺寸/样式说明，也不擅自批量改写已有名称。
- 本次为 skill-creator 指导下的规范更新，不修改页面、业务合同或真实项目数据；当前已有名称保持不变。

### 10.2.19 品牌与文章来源（2026-09-09）

- 按用户批注将 Skill 示意图标和品牌名从顶部项目身份中拆出，放到侧栏底部；顶部保留项目名、返回与语言。增加 Inspired by Minor Adventures 和《The Making of Cursor’s Icons》原文链接，已核对来源页面。
- layout 指导下沿用现有黑白灰、间距与分组，不增加中央区域内容或主观评价。先取得旧顶部图标导致的失败测试，再实现；本地 HTTP 完整 E2E 通过，桌面与 390px 截图已检查，无横向溢出。机械扫描因解析依赖缺失降级，不当作完整视觉证明。
- 仓库新版预览在 `http://127.0.0.1:52930`（PID 93370）保留，已打开并核对 HETAO 项目信息及底部来源链接。未替换全局安装、未修改真实图标内容、未提交或推送；测试浏览器与临时测试服务已由测试收尾关闭，既有安装版服务未擅自终止。

### 10.2.20 侧栏层级与间距修整（2026-09-09）

- 按用户确认的方向，使用 layout 收紧顶部间距、移除返回及语言按钮常态边框、移除项目信息上下的横线分区；项目名与导航文字统一对齐，方案分组标题改为较轻的 12px 次文色。底部品牌和文章来源不变，没有新增导航层级或改写项目名称。
- 本次纯 CSS 视觉修整，构建及本地 HTTP 完整 E2E 通过；已检查实际 HETAO 页面和 390px 截图。原仓库预览 52930 已刷新并保留，未新建服务、未替换全局安装、未提交或推送。

### 10.2.21 项目库导航去重（2026-09-09）

- 按用户确认去掉项目库侧栏重复的项目库标题、项目分组和选中项；保留语言及底部品牌来源，进入项目时恢复项目身份和导航。使用 distill 精简重复入口，不新增功能。
- 先取得重复标题仍可见的失败测试，再修改渲染；构建和本地 HTTP 完整 E2E 通过，实际 52930 项目库已刷新并检查。未修改图标数据、未替换全局安装、未提交或推送；原预览继续保留，测试进程由测试收尾关闭。

### 10.2.22 项目库独立布局（2026-09-09）

- 修正上一轮只删导航内容却保留空侧栏的问题。项目库及未连接目录页面使用无侧栏布局：右上角语言、全宽主内容、左下方自然流页脚；进入项目恢复原侧栏。复用并移动真实控件节点，避免重复按钮与事件绑定。
- 先记录项目库仍预留 224px 导航列的失败测试，再实现。构建、本地 HTTP 完整 E2E 与项目库→项目→项目库往返检查通过；实际 52930 页面、语言切换及 390px 截图已检查。既有预览保持运行，无新增服务；未安装、提交或推送。

### 10.2.23 全局内容宽度与对齐（2026-09-09）

- 根据用户要求将布局修正覆盖项目库、项目信息、图标列表、图标结构和应用场景，不只修首页。共用主内容容器：集合 1120px、阅读 760px、详情 1000px，在可用主区居中且内部左对齐；项目库语言与品牌页脚共用边界。
- 项目卡片改为最大 340px 的网格，日期下置；图标条目固定 144px。保留导航结构、侧栏和预览变体尺寸稳定性。layout 规则用于统一容器，而非把所有内容都改成居中文本。
- 新增容器居中、卡片限宽、多项目并排与窄屏无溢出回归；先取得缺少共享容器的失败测试，再实现。构建及本地 HTTP E2E 检查覆盖上述页面，真实 52930 预览刷新，宽屏和 390px 截图检查。未修改真实项目数据、全局安装，未提交或推送；沿用原预览服务，测试自行清理隔离浏览器和数据。

### 10.2.24 清理刷新首屏残留（2026-09-09）

- 删除静态模板中写死的“项目库”身份标题。初始工作区隐藏，首屏只显示加载状态；路由、数据与布局渲染完成后再展示工作区，避免所有页面刷新时先闪出侧栏。启动失败时停止加载提示并保留错误提示。
- 通过阻塞 bootstrap 请求复现旧模板侧栏可见的失败测试，再验证加载期间无旧标题/侧栏、加载后页面恢复；构建、本地 HTTP 完整 E2E 通过，加载状态截图和实际 52930 刷新检查完成。保留预览，未安装、提交或推送。

### 10.2.25 语言入口统一归属（2026-09-09）

- 按用户确认将语言按钮移到 Skill 品牌旁，项目库页脚和项目侧栏底部共用同一个节点，保留语言持久化和键盘操作。项目库隐藏无内容的顶部导航行，返回按钮不变。
- 新增品牌区按钮归属、垂直对齐与顶部隐藏回归，先验证旧位置失败，再构建并运行本地 HTTP E2E；实际预览及宽窄屏截图检查。未提交、推送或替换全局安装，沿用 52930 预览。

### 10.2.26 项目主线与复制给 Agent（2026-09-11）

- 已确认需求：项目保存主方案 ID、确认时间与用户答复来源；打开项目、读取项目及当前上下文附带主线 ID、当前名称、确认时间。仅明确答复可设置／切换／取消，倾向和查看不自动确认；图标修改不取消主线，默认不删除、归档其他方案。
- 增加 `set_primary_scheme`，替代未接 UI 的 `set_preferred_scheme`。界面仅主方案标注“已确认”，备选使用次文色，查看高亮独立、均可点击。不新增逐图标审核及待审核／需修改／待复核状态；旧低层版本审核接口仅保留历史能力，Skill 常规流程不调用。
- 增加只读 `get_agent_handoff` 与“复制给 Agent”按钮：列表为整个方案，结构为当前尺寸，应用场景为整个图标。返回授权目录、确切身份、源路径、哈希和主线信息；不导出、不携带令牌或端口。接收 Agent 经 Skill、HTML 同意与本地权限读取当前源和 SVG，不擅改源数据。剪贴板失败显示可手动选择的只读指令，中文英文均支持。
- TDD：先取得缺少主线／交接合同的失败测试和缺少按钮的浏览器断言；核心、合同、集成全量 71/71 通过。新增浏览器测试覆盖主线标志、备选可访问、三种复制范围、剪贴板失败、语言与 390px 无溢出；HTTP／原生 WebMCP 完整流程、Worker 同核和 600 图标页面均通过。大库旧断言要求侧栏标题与内容标题同高，HEAD 原界面也失败；按已确认独立侧栏布局改验内容区标题、操作组与筛选栏对齐后通过，缓存详情 p95 24ms。截图见 `.impeccable/review/primary-desktop.png` 和 `primary-mobile.png`，已实际查看；Skill 校验与界面机械检查通过。
- 验收使用 `.qa/primary-build` 隔离构建和合成项目，不替换 `dist/app`、全局安装或 HETAO 数据。不转换旧 preferredSchemeId 项目，新合同不兼容旧字段；旧预览与数据保持原状。测试服务及浏览器均由测试关闭，本轮未新增常驻服务，既有 59153 预览保留。未提交、推送、安装或部署。

### 10.2.27 新版隔离交互验收（2026-09-11）

- 用户重申对外发布前不迁移旧数据，并同意以新版结构新建测试项目。使用仓库 `.qa/mainline-demo/library` 独立合成库，没有读取、复制或转换 HETAO 的旧图标文件，没有替换全局 Skill。
- 新建“主方案功能验收 合成示例”，以合成图标和合成确认验证 A 已确认、B/C 保留；合成确认不代表真实 HETAO 项目状态已更新。真实 HETAO 的 A 选择目前仍仅有对话确认。
- 新容器 56414 已实际打开，截图检查 A 标记与备选淡化、复制入口；点击按钮出现“已复制”。合同读取验证项目主线、方案交接目标及源哈希，指令不含令牌和临时端口。回执位于 `.qa/mainline-demo/evidence.json`，不纳入提交。
- 验收预览进程 64484 保留供用户查看；旧 HETAO 预览 59153（进程 28222）继续保留。没有删除数据，未提交、推送、安装或发布。

### 10.2.28 新界面直接使用现有 v3 图标（2026-09-11）

- 用户明确澄清不迁移不等于禁止新界面打开相同图标。本次纠正 10.2.26–27 的过度限制：同一 v3 项目缺少新增主方案字段可直接读取，内存默认未选择；保留 preferredSchemeId 但不将旧偏好当作确认。未知格式仍拒绝，不增加迁移工具。
- TDD 先复现现有 v3 项目读取失败，再验证读取、打开、交接均不改写磁盘，明确确认后只保存项目记录、几何不变；全量 71/71 通过，隔离构建与 Skill 校验通过。
- 新界面直接连接原 HETAO 目录，按用户明确答复调用 set_primary_scheme 将 A 作为已确认。全库文件哈希对比仅 project.json 改变，所有原图标、方案和编译数据保持原样。真实页面已检查 A 标记、B/C 保留、两项图标和三个变体交接，复制按钮实际显示已复制。证据 `.qa/hetao-current/evidence.json` 不提交。
- 当前交付预览 57720（PID 66535）保留；已向原旧预览 PID 28222 和示例 PID 64484 发送 TERM。没有删除任何项目或测试数据，没有替换全局安装，未提交、推送或发布。

### 10.2.29 WebMCP 网关与安装一致性（2026-09-11）

- 针对 Codex 宿主配置超限，不删除 96 个业务能力，也不另造业务核心；原生 WebMCP 从逐项注册改为 5 个固定网关：工作流、紧凑目录、单项 schema、只读调用、写入调用。读写分离保留宿主注解，单项输入继续由唯一合同严格校验，HTTP 路径保持原业务操作名。
- 版本提升至 `0.1.0-dev.2`，合同包新增 `schemaRevision: 2`。健康检查、bootstrap、候选版本和 manifest 同时报告 contract／format／schema revision 与 build ID；页面显示版本和 build ID 短码。未知字段错误增加具体字段，页面错误附业务操作和字段路径。
- 新增本地安装检查与受控替换脚本：从当前源码在临时目录构建，比较 manifest build ID；安装要求明确目标和备份目录，先复制完整备份，再在目标父目录原子切换，不停止运行实例、不读取或迁移图标库。源码、候选、安装和运行实例不再只靠相同预发布版本判断。
- TDD 先取得逐项注册、缺少 schema revision／字段名和旧版本包的失败证据。核心、合同、集成 73／73 通过；构建后 5 项浏览器 E2E 通过，覆盖本地 HTTP、Chrome 原生 WebMCP 网关完整生产/重开、600 图标页面和 Worker 同核。
- 经用户本次明确授权替换全局 Skill。安装前检查准确报告旧 `0.1.0-dev.1`／build `f7b72b51…` 与当前候选不一致；旧目录完整备份至 `<USER_HOME>/.codex/skill-backups/make-product-icons-2026-09-11T06-07-50-913Z-a2714c71`，原子切换后复查为 `0.1.0-dev.2`、schema revision 2、build `0c512514…a2714c71`，与源码临时构建一致。安装过程未停止既有服务、未连接或修改 HETAO 图标库；正在运行的旧实例不因此冒充新核心。
- Codex 真实宿主复验已通过：从已安装包启动 PID `48273`、`127.0.0.1:52990` 的新实例，health 和页面同时显示 `0.1.0-dev.2`、schema revision 2、build `0c512514…a2714c71`。内置浏览器实际枚举且仅枚举 5 个网关；`get_workflow`、`list_operations`、`get_operation_schema` 与 `read` 均可调用，`read` 成功读取含三个主方案字段的项目、两个图标、设计规则、空图元列表和三项预览。三项 content hash 与调用前一致。旧 PID `97903`／端口 `57141` 未停止，复验未调用写网关。A4-02 关闭。

### 10.3 排期口径

按以上依赖顺序安排工作包，不在尚未验证共享几何和目标宿主的情况下给出虚假的精确完工日期。A1 完成后根据实际算法工作量更新剩余工程估算；这不改变范围，也不重新要求确认已定产品需求。

阶段一交付：完整 Skill 候选包、版本清单、标准操作文档、测试与视觉证据。全局安装/替换须有明确授权。

阶段二交付：同源网站产物、完整 Skill 下载、实际更新源、双端接续报告和真实部署验收。公开发布须有明确授权。

两个阶段都不包含旧数据迁移、并发写入校正或额外平台化建设。开发、验证、安装、部署和进程收尾分别报告。

### 首页使用入口与视觉扩展（2026-09-12）

- 首页主标语改为“超好用的图标设计工具”，新增品牌角色、正反色展示、在线及本地两条引导路径；改动仅作用于网站启动页。
- 在线指令读取同源完整 Skill，无需本地安装；发布构建从同一候选包提取 SKILL.md、引用、版本和合同。目录控件仅在 Skill 就绪后显示，不把打开网页视为用户同意。
- 本地指令使用现有校验过的预览包。公开仓库地址仍未提供，不公开私有仓库，不生成假安装链接。
- 新行为先以失败 E2E 描述；实现后首页与原生 WebMCP 完整流程 2 项 E2E 通过。真实 Chrome 1280、858、390px 检查无横向溢出，复制失败、正反色和英文切换已验证。
- 本次视觉复核由当前 Agent 执行，未启动子 Agent。本地构建及预览已完成，本次首页改版尚未部署，不表示线上页面已更新。

### 官网与独立工作区（2026-09-12）

- 按用户确认拆为 / 官网、/studio/ 工作区，后者可收藏并直接打开。查看、导航、导出走显式操作允许列表，不要求 Skill；创建库、项目、几何写入及设计确认继续要求真实 Skill 声明。
- 工作区初始化后恢复浏览器目录引用：授权有效则自动打开，待授权提供重新授权按钮；断开遗忘引用、不删文件。无需 Agent 重新导入。目录能力与 WebMCP 分开判断。
- Skill 引用及官网复制指令已指向 /studio/，只修改仓库内 Skill，未替换全局安装。
- 验证：82 项单元/合同/集成测试通过；3 项页面 E2E 通过，包括默认无 WebMCP 的 Chrome 打开、刷新详情、SVG 下载和断开，以及原生 WebMCP 生产回归。磁盘句柄 E2E 使用 OPFS 合成夹具，真实外部目录权限矩阵仍未完成；不将其称为完整权限验收。
- 桌面与手机工作区入口已真实渲染检查。预览保留在 http://127.0.0.1:53058/，工作区 /studio/；仅重启本任务的静态预览 PID 56186，新 PID 61277。本次未提交、未推送、未部署线上。

### 首页说明精简（2026-09-12）

- “开始做图标”在当前页面平滑滚动到用法说明；系统要求减少动态效果时直接定位，保留键盘焦点与锚点。
- 按用户要求删除在线/本地两段复制指令、按钮、回退输入框与重复步骤清单，改为各一段简短说明。工作区入口、设计能力与 Skill 文件保留。
- 对应页面 E2E 先失败后通过；桌面与手机截图检查完成，预览构建已更新。未部署线上。

### 首页按钮纠正（2026-09-12）

- 用户澄清：多余的是首屏“开始做图标”，而不是下方两个指令按钮。已移除首屏按钮及其滚动逻辑，恢复在线/本地复制按钮与剪贴板失败回退；用法说明保持简短。本条替代上一节对按钮的误解。
- 首页 E2E 验证首屏无重复入口、两条指令复制及失败回退；桌面和手机视觉检查通过，当前预览已重建，未部署线上。

### 首页文案精简（2026-09-12）

- 删除重复的首屏宣传句和用法引言；两条入口统一为短标题、适用条件、一句操作说明。精简能力提示、安装来源和本地存储说明，同步英文，保留两个复制按钮及完整 Agent 指令。
- 首页 E2E、构建通过，桌面和手机实际渲染检查通过；仅更新本地预览，未部署。

### 首页在线自动进入工作区（2026-09-12）

- 在线入口说明改为复制指令给 Agent，准备就绪后自动进入工作区。首页在浏览器能力可用时注册同一套原生 WebMCP 网关；只有 acknowledge_skill 成功才进入 /studio/，复用原 Worker，不丢失加载及同意状态。复制动作本身不跳转、不声明同意，目录仍需用户授权。
- 原生 WebMCP E2E 先失败后通过：覆盖拒绝同意不跳转、成功自动跳转、会话保留及后续生产导出；首页与无 WebMCP 查看回归一并通过。桌面、手机渲染检查及构建通过。预览已更新，未部署线上。

### 两种用法对比（2026-09-12）

- 两侧使用说明下增加三个对应勾选要点：WebMCP 支持要求、本地 Skill 安装要求、项目数据不上传到 Boplet。最后一项明确限定 Boplet，不代替用户所用 Agent 的数据政策。保留原复制行为，删除重复的底部存储声明，同步英文。
- 首页 E2E、构建与桌面和手机渲染检查通过；本地预览已更新，未部署。

### 官网更新部署（2026-09-13）

- 按用户授权通过现有 Cloudflare CLI 部署至 https://boplet.app，版本 `111b4910-97ba-41a4-8357-75d11a04480d`。包含最新首页、独立工作区、首页原生 WebMCP 启动流程及完整 Skill 预览包。
- 线上首页、/studio/、JS、CSS、Skill 与发布清单均返回 200，内容与本次构建逐字节一致；下载 ZIP 的 SHA-256 核验通过。真实浏览器确认三项对比与工作区入口正常，无页面脚本错误。
- 本次仅部署，未提交或推送 Git；验证浏览器已关闭，原本地预览保留。外部目录权限全矩阵验收仍沿用前述未完成状态。

### 官网趣味视觉更新（2026-09-13）

- 依据用户认可的 Mobbin 参考方向，将首页调整为暖黄色首屏、倾斜的图标创作台和粉色实际尺寸样张；沿用已确认品牌图标，增加构造查看切换，保留反色演示。两种使用方式及编辑器不变，样式限定首页。
- 构造交互先补失败测试再实现；首页、独立查看、原生 WebMCP 工作流共 3 项 E2E 通过，网页构建通过。检查桌面和手机实际渲染，1957、1280、858、390 宽度无横向溢出。
- 仅更新本地预览，未部署、提交或推送。

### 首页构造展示修正（2026-09-13）

- 删除用户指出的演示说明句；移除百分比定位的假构造框与节点，直接从品牌 SVG 路径计算端点及贝塞尔控制线，随同一 SVG 缩放和旋转。
- 首页 E2E 先失败后通过，检查说明及假节点移除、真实路径起点与控制线存在；构建通过，实际渲染确认构造贴合轮廓。仅更新本地预览，未部署。

### 首页示例图标尺寸统一（2026-09-13）

- 修正横条内四个示例图标的实际轮廓占比：32 单位画布、24 单位主轴、中心 (16,16)，保持统一描边及各自形状比例。不改上方 16/32/64 尺寸演示。
- 轮廓尺寸及中心回归先失败后通过；首页 E2E、构建通过，桌面和手机渲染检查完成。仅更新本地预览，未部署。

### 趣味首页上线（2026-09-13）

- 按用户授权部署至 boplet.app，版本 `c262b272-63c6-442f-acf3-f352ab3f452b`。包含暖色首页、真实路径构造、统一示例图标及最新中英文介绍文案。
- 线上首页、脚本、样式、工作区入口和下载清单与本次构建逐字节一致；浏览器刷新确认最新文案及新首页结构已加载。未提交或推送 Git，本地预览保留。

### 工作原理说明（2026-09-13）

- 最新简洁版已部署至 boplet.app，Cloudflare 版本 `1c531f1d-e066-4662-9ac6-69b0eaad1318`。包含真实三方案及 A 前后对比、强化开始使用入口、不含五步流程条。线上首页、JS、CSS、工作区入口和下载清单均返回 200 且与本地发布产物一致；浏览器已确认新版内容。Skill 下载包哈希未变，未提交或推送 Git。

- 用户对比后最终选择去掉五步流程条，恢复简洁版；三个真实过程图和精简说明不变。同步删除流程样式及更新回归。

- 按最新反馈恢复案例下方的五步完整流程，保留精简说明。中英文同步，首页 E2E 与构建通过，本地预览更新，未部署。

- 排版收尾：开始使用增加首屏向下锚点、强化标题及两个复制按钮；工作原理删除重复图注与流程条，保留真实过程图及简短说明。首页 E2E（含锚点可见性、复制及四档视宽）和构建通过，桌面/手机已检查。本地预览更新，未部署。

- 补齐真实过程稿：只读恢复品牌项目 A 探头、B 搭肩、C 举块的 32px 初稿，通过共享几何核心生成静态展示图形；A 初稿与已采用的方形镂空版本并排对比。未复制项目文档、未更改原项目。更新回归覆盖三个不同初稿、A 对比图复用及尺寸交付，首页 E2E 与网页构建通过，桌面/手机截图已检查，本地预览已刷新；未部署。

- 按用户选择，先用 Boplet 品牌图标贯穿真实案例：需求、真实路径、尺寸检查共用已发布品牌 SVG；原图标横条改为案例入口。保留开始使用在前、工作原理在后的顺序。首页 E2E 通过（含路径一致性、真实节点、四档视宽、语言与复制入口），网页构建通过，桌面与手机渲染已检查，本地预览已更新；本次未部署。

- 在“开始使用”之后增加用户确认的详细工作原理：Skill 方法、网页工具、用户对话反馈、需求到交付流程与本地项目重开；同步英文。桌面分栏阅读、手机纵向展开，不增加折叠或卡片。
- 区块顺序回归先失败后通过，首页 E2E 与构建通过，桌面和手机实际渲染检查完成。仅更新本地预览，未部署。

- 根据用户对阅读负担的反馈，改为三幅几何图解（统一规则、轮廓到成品、反馈调整圆角），每幅保留一句分工说明；保留五步流程与本地保存说明。中英文同步，位置仍在“开始使用”之后。首页 E2E、构建及桌面和手机渲染检查通过，未部署。


### 新设计复用项目库指引修正（2026-09-15）

- 已核实 FileBox 任务另建 `filebox-ui-v3` 项目库，导致本地容器与已有 HETAO 库隔离；核心本身支持同库多项目。
- 修正仓库 Skill、运行与恢复、数据与维护指引：新产品在已有授权库调用 `create_project`，不按产品或对话另建库；区分库根目录、父目录和项目数据目录，多个候选不明确时确认目标。未知格式不得静默另建目录绕过。
- Skill 格式验证通过，项目集成测试 12 项通过。此次仅修改指引，未改业务或界面代码，未更换全局安装、发布、合并或移动真实库；当前已分散项目的本地切换问题尚未消除。未启动服务。

### 统一根目录下直接保存项目（2026-09-15）

- 用户确认磁盘层级采用 `Icon Projects / 各项目文件夹`，不保留内部 `projects` 中间层。同步更新架构方案与 Skill：存储授权和页面位置指向共用根目录，新设计在其下创建项目，版本或对话不另建库。
- 新库标记 `projectLayout: direct`，项目 ID 目录直接位于根目录；核心读取、生产、历史、导出及 Agent 交接路径均按库布局解析，网页目录识别共用同一布局规则。显示名称与目录 ID 分离。
- 已有无标记 v3 库仍在原 `projects/p-…/` 位置读写，不补写标记、不自动迁移；schema revision 提升至 3，候选版本为 `0.1.0-dev.4`，旧安装版不能读取新布局。
- 新增根目录保存、重开切换回归，先出现预期缺失路径失败，再实现通过；增加旧布局保持原样与网页发现回归。89 项单元、合同及集成测试、12 项端到端测试全通过，本地和网页构建通过，Skill 校验通过。
- 真实 Chrome 合成数据验收确认两个项目位于根目录，列表可切换且存储位置不变，保存、编译、导出、刷新重连正常；已检查桌面与窄屏截图。测试运行已退出，无需保留测试服务。
- 尚未更新全局 Skill、整理真实项目、发布、提交或推送；当前真实 `Icon Projects` 下仍是原有分散库，不能把本次代码验收视为真实目录整理完成。

### 本机 Skill 更新与授权的一次性项目迁移（2026-09-15）

- 用户明确允许本次旧项目迁移，作为此前“不迁移真实数据”约定的本次例外；正常启动仍不自动迁移。不新增自动合并或通用旧格式迁移入口。
- 查明旧页面使用安装版 `0.1.0-dev.2`，另有同核心的临时预览。构建并验收唯一 `dev.4` 候选后更新本机安装；安装、当前源码构建、健康检查与页面资产均匹配 build ID `24f54eedefddaaf35f5c51c338e7a58c7b1021965e914cd3a4851761d60e4a7b`，schema revision 3。
- 四个已确认绑定旧项目目录的服务已精确停止。701 个原文件完整备份并逐文件哈希核对后，将三个现行格式项目及两个更早项目统一到根目录下，最终为 5 项目、11 方案、60 变体。现行项目保留 ID、图标和历史；旧格式转换后保留全部原始资料，确认状态仅转录原文件证据，不伪造新确认或主方案选择。
- 新包独立启动、离线重开、导出和列表重排回归通过。迁移后全量读取/预览及旧项目交接路径校验通过；10 个旧变体进行了原尺寸与 8 倍渲染对照，存在轻微圆形/圆角边缘抗锯齿差异，原始 SVG 保留。详细指标与迁移脚本仅存本机备份，不提交用户数据。
- 已在实际浏览器确认五项目列表、FileBox 详情返回、跨项目切换、刷新恢复及根目录位置；新版服务与页面保留供用户继续使用。旧端口已关闭，未提交、推送或线上发布。

### 目录修正与列表重排修复提交准备（2026-09-15）

- 用户授权将相关 Skill 改动推送远程，再恢复中断的 FileBox 列表修复任务。此次提交包含统一目录布局、Skill 规范、版本合同、路径解析与列表首帧限宽修复；视频工程及其他研究文档不纳入。
- 提交前重新构建核对本机安装，`dev.4` / schema revision 3 / build ID `24f54eedefddaaf35f5c51c338e7a58c7b1021965e914cd3a4851761d60e4a7b` 与当前开发源码一致。沿用有效的 89 项基础回归、12 项端到端回归，以及随后完成的完整安装包和列表重排两项回归证据。
- 恢复任务应加载当前安装版，复用统一根目录内的原项目 ID，在新版实际页面完成剩余验收；不重复建项目，不恢复旧分库目录或临时页面。远程推送与任务恢复状态以后续回执为准。

### FileBox 返回列表安装版验收（2026-09-15）

- 恢复原 FileBox 项目和「A 安静的工作台」，复用安装版服务 `http://127.0.0.1:57799`，未创建新项目。安装检查返回 `current: true`，dev.4 / schema revision 3 与当前源码构建完全一致；修复已包含于提交 `881a829`。
- 实际内置浏览器从详情返回列表，120 次动画帧采样中捕获 27 个列表帧：从首次出现即保持 1120px 列宽，16 张卡片位置只有一种状态；随后全部 16 个缩略图完成，卡片位置保持不变。刷新后原项目、方案和列表恢复，截图确认布局正常。
- 验收前后原项目 266 个文件逐一哈希相同。保留本任务页面及既有安装版服务（PID 27498）；没有恢复旧端口或临时混合包，没有新增待清理的应用服务。

### 返回列表复用图标缩略图（2026-09-15）

- 返回列表时同步填入已缓存 SVG，跳过未变化图标的预览请求；缓存最多 192 项，失败预览允许重试。列表查询提供包含矩阵版本、方案规则和共享组件内容的预览标识，内容变化后重新读取，空方案没有规则也能正常浏览。
- 新增回归先复现未修改仍发送预览请求，再验证缓存命中、几何修改、规则和共享组件失效。89 项基础测试通过；完整浏览器回归 14 项中发现并修复空方案问题，随后相关 3 项重跑通过，其余 13 项已有通过证据。构建与独立安装包离线验收通过。
- 本机安装已更新并备份旧包：版本仍为 dev.4 / schema revision 3，新 build ID 为 `a31d850c1cc00f6de578a37bffb21cd2ec4b7ee6effbb3684670c4dae1d75e91`，候选、安装检查和服务健康状态一致。
- 原 FileBox 实际页面详情返回时捕获 85 个列表帧，每帧均显示 16 个 SVG，列表宽度始终 1120px；同一观察区间 `preview_icon` 请求为 0。保留新版服务 62223（PID 37158）与本任务页面，明确归属的旧服务 57799 已停止；原图标文件未修改。用户授权代码推送和本机更新，不包含官网部署或 Release 发布。

### 详情页上下外边距微调（2026-09-15）

- 根据用户截图中返回按钮过于贴顶的反馈，将详情页上下外边距统一为 24px，取消针对返回按钮的顶部光学扣减；其他页面保持原间距。
- 三种屏宽的现有间距回归通过，本机安装同步为 dev.4 构建 `72d6c199`，保留新版服务 63067（PID 39722）。此次微调未提交或推送。

### 全局顶部留白统一（2026-09-15）

- 用户明确纠正此前只修改详情页的范围：顶部留白属于全局规则。所有工作区页面、左侧导航和右侧检查栏统一使用 `--edge-visible:24px`，移除各模块光学扣减与粗指针归零例外，同步更新设计规范。
- 现有浏览器回归先复现项目库仍为 8px，再验证五类页面及两种头部布局在 1911、858、390px 下均为 24px；回归通过。实际安装版检查项目库、列表及三栏详情，三栏顶部计算值均为 24px。
- 本机安装同步 dev.4 构建 `cf325371`，旧安装已备份；预览作为独立后台服务运行于 49926（PID 20008），保留原 FileBox 页面，旧服务 64985 已停止。本次尚未提交、推送。

### FileBox Skill 设计与交付测试恢复（2026-09-15）

- 复用原项目和方案，实际 WebMCP 验证 32/32 变体通过，整套导出 ready，32 份 SVG 逐项读取校验并生成本机 ZIP；新增导出记录，未修改原图标几何或替换 FileBox 应用资产。
- 完成 16/18px 原尺寸浅深底与邻接文字验收板，核实六个共享组件及实例使用。发现场景页缺少 18px 原尺寸对比、18px 下载箭头存在未记录的组件例外；视觉候选问题与真实产品验收边界记录于《图标设计方法原点对照审查》。七项合成生产/维护/恢复/导出回归通过。
- 保留原工作区服务 49926（PID 20008）与独立验收板 64728（PID 69159），验收 HTML 和 SVG ZIP 存在本机 Icon Studio Reviews 目录。未提交、推送或发布；尚未完成真实产品内替换验收及基于反馈的细化扩库。


### 2026-09-15：FileBox Skill 测试缺口修复

- 场景尺寸对比、反色自动合并已绑定用途的实际尺寸，去重并排序；FileBox 的 18px 浅深底均直接使用 18px 源变体，保留放大检查。补充 18/20px 回归，先失败再实现；maintenance、production、transforms、recovery-export 共 8 项通过。
- 经用户“修复吧”授权，将 FileBox 下载 18px 的独立箭头路径替换为 prim-arrow-down-18 实例，平移 [6.5,9.5]。已编译、整套导出 32 SVG，逐文件对比修复前导出内容全部相同，外观无变化；反馈已处理，未代用户记录设计认可。
- 已在真实页面确认 18px 浅深底样本和点击进入 18px 结构变体。Skill 方法文档补充原尺寸验收和光学例外记录要求。
- 本机安装核对一致：0.1.0-dev.4，build 尾号 1039b2dd；预览 http://127.0.0.1:49522，独立服务 PID 75858 保留供验收，旧 PID 20008 已停止。验收 HTML 和 32 SVG ZIP 更新于 Documents/Codex/Icon Studio Reviews/20260915-filebox。未提交、未推送；未替换原生 FileBox 界面。

### 2026-09-15：FileBox 业务使用第一轮

读取当前 FileBox 源码并观察原生主窗口和文件菜单，确认标题栏 16px、通用操作 18px，下载使用圆形下载标记，右键菜单主要依靠文字。交付独立业务试用页 Documents/Codex/Icon Studio Reviews/20260915-filebox/业务试用.html，预览 http://127.0.0.1:64728/business.html。使用实际导出的 Skill SVG 和现有 Phosphor SVG 作同布局对照，文件均为示例。

实测文稿筛选、反向排序、选择文件、空格预览、关闭返回、浅深色及方案切换。状态保持正常。观察到下载与中转的托盘轮廓在相邻导航中可能降低区分度，需进一步设计比较，不代用户认可。此页是业务模拟，不是原生接入；本轮未执行需求变更生产，完整业务闭环仍待验证。测试说明与页面同目录保存。未修改原生 FileBox 或图标源，未提交推送。沿用既有预览服务，已有 FileBox 进程保持运行。

### 2026-09-15：容器内图标列表双模式

按用户确认，直接在现有图标列表增加网格/列表切换，不新增业务场景工具。列表按语义逐行、尺寸/样式/轻重分列，分别显示原尺寸浅深底；缺失变体留空。共用搜索、筛选、分页和内容版本缓存，点击样本进入确切变体，详情返回保留模式。模式偏好仅保存在浏览器，不写入图标源或改变业务合同。

三项浏览器回归通过（网格布局、缓存失效与复用、列表原尺寸与返回切换）。真实 FileBox 库确认 16 行、64 个浅深底 SVG，18px 样本进入正确变体，搜索下载得到单行列表。修正模式按钮和深底样本的悬停对比度。已安装并核对 build f73de22b；最终预览 http://127.0.0.1:54742，PID 92099 保留，替换的旧容器实例已停止。未提交、未推送，未修改图标源。

### 2026-09-15：详情模式切换保留右栏

用户指出结构切换到应用场景时右栏消失、内容宽度跳变。先以浏览器回归复现 280px 差异，再让场景模式保留相同右栏，显示当前语义名称、说明与图标范围的“复制给 Agent”；不继承结构选区或显示无关图层属性。四项相关浏览器回归通过，中转图标真实页面往返观察通过。已安装 build a6502849，预览 http://127.0.0.1:55381，独立 PID 93372 保留；旧 PID 92099 已停止。未提交推送，未修改图标数据。

### 2026-09-15：详情右栏顶部统一

按用户进一步澄清，两种详情模式共用完全相同的规格信息、下载 SVG 和复制给 Agent，只有图层/属性随模式显示或隐藏。浏览器按图标记住当前变体，场景右栏明确显示对应尺寸；两个按钮均使用该变体。场景内容仍覆盖整个语义图标，几何选区仍清除。同步更新 Skill 按钮范围说明。四项浏览器回归通过，增加顶部 HTML 完全相同的断言；真实中转 18px → 场景 → 结构往返保持 18px。安装 build f8001484，预览 http://127.0.0.1:55708，PID 94433 保留；旧 PID 93372 停止。未提交推送。

### 2026-09-15：列表和详情滚动位置分离

用户复现列表底部进入警告详情仍停在底部。根因是 SPA 切换复用文档滚动位置，navigate 未重置或恢复。现按页面目标及列表模式、搜索、标签、分页记录滚动；新进入详情置顶，返回列表恢复原位置，浏览器前进后退显式恢复目标页面位置。焦点恢复使用 preventScroll，防止刷新抢动滚动。

失败测试复现进入详情 scrollY=357，修复后 5 项相关浏览器回归通过，覆盖网格/列表两种模式和历史往返。真实 FileBox 警告路径验证：列表 scrollY=376 → 详情 0 → 返回 376。安装 build ef1c06ba，预览 http://127.0.0.1:57656，PID 7258 保留；旧 PID 94433 已停止。未提交、未推送，未修改图标源。

### 2026-09-15：网格列宽填满内容区

用户指出固定卡片列宽使右侧留下多余空间。将最终网格规则改为 auto-fill + minmax(min(100%,144px),1fr)，各列均分可用空间，保留既有间距，末行沿用整行列宽。新增 1120/818/390/140px 宽度的右边缘对齐和列宽一致性测试，先复现右侧 64px 差值，修复后 7 项布局及导航回归通过。本机更新 build 9a2f4ae3，预览 http://127.0.0.1:59136，PID 15240 保留，旧 PID 7258 已停止。未提交推送。

### 2026-09-15：数量并入列表标题

将结果数量从筛选栏移到图标列表标题旁，采用次级字号与基线对齐；搜索和筛选继续更新同一统计，不再独占右侧空隙。五项相关浏览器回归通过。本机 build 309c7a21，预览 http://127.0.0.1:59698，PID 21403 保留；旧 PID 15240 已停止。未提交推送。

### 2026-09-15：缩小图标搜索框

按用户反馈，将桌面搜索框从 280px 缩至 180px，窄屏既有自适应规则保留，让标签和模式切换有足够同行空间。纯尺寸调整以真实界面检查验证。本机 build 5b08a2f6，预览 http://127.0.0.1:60053，PID 25723 保留；旧 PID 21403 已停止。未提交推送。

### 2026-09-17：继续 Skill 与容器流程回归

核对当前源码与本机安装 build 5b08a2f6 一致，无需重新安装。原端口 60053 已失效，恢复同一授权项目库的本地容器，当前预览 http://127.0.0.1:53967，PID 36685 为交付保留。真实 FileBox 页面成功连接，下载图标 18px 切换应用场景仍保持当前规格；通过原生 WebMCP 执行 validate_scheme 并追踪最终回执，32 个变体全部通过。

当前 90 项单元/合同/集成测试与 18 项浏览器回归通过，包含反馈修改与恢复、列表缓存更新、网格/列表往返及滚动、布局宽度。进一步扩展既有右栏回归，使用隔离合成数据验证第二尺寸 18px 的场景复制内容包含正确变体、不混入默认变体或会话令牌，实际下载 SVG viewBox 为 18×18，刷新及返回结构仍保持 18px。扩展后的单项回归通过。首次下载测试因合成数据未编译而超时，补齐标准编译步骤后通过，未修改产品实现。真实内嵌浏览器显示复制成功，但自动化剪贴板读取未返回内容，因此交接正文与下载字节的断言由隔离 Chrome 回归提供，不将按钮提示替代内容核验。

本轮只补测试和进度记录，未修改真实图标源、提交或推送；一次性测试实例均按 finally 关闭，用户预览服务保留。这些是功能与合同证据，不替代图标视觉质量的人工判断。

### 2026-09-17：准备发布 0.1.0-dev.4

用户授权再次发版。本次发布包含统一项目根目录、缩略图缓存与列表返回修复、原尺寸浅深底列表、全局留白、详情右栏一致性和独立滚动位置；同步 Skill 查看与交接规范。沿用本轮 90 项基础测试、18 项浏览器测试以及随后扩展的 18px 复制/下载/刷新回归通过证据。版本保持开发预览，Windows 实机与完整跨运行时目录权限矩阵仍待验证。宣传视频相关内容不属于本次发布。发布候选、GitHub 下载验证结果将在后续记录；此条不代表已发布或官网已部署。

### 2026-09-17：0.1.0-dev.4 GitHub 发布完成

发布源码提交 `b44d48d`，标签 `v0.1.0-dev.4`，已推送 main 和标签。干净独立工作树生成唯一候选，归档至 `outputs/release-dev4-20260917`；Node 22.23.1、GitHub CLI 2.97.0。版本 0.1.0-dev.4，schema revision 3，build ID `sha256-752c221d52986ea86e6c0e0b44bee0e9bc0c014195e9cae1dfb081365b08a2f6`。ZIP 296686 字节，SHA-256 `94df003e58d23155be347f608cbb46c2225370ceba6246a21fdf5ce34b35ddbf`。

[GitHub 预发布](https://github.com/walnut-a/icon-studio/releases/tag/v0.1.0-dev.4) 已公开，包含完整 Skill ZIP、release.json 和 sha256.txt。候选通过清单/ZIP 校验及脱离仓库的安装、启动、导出、重开和保留数据验收。发布后通过无 GitHub 登录凭据的 curl 重新下载三份附件，哈希一致；对下载解压的包再次执行独立安装验收，通过。没有重新构建或覆盖已发布附件。

本机安装检查与发布候选完全一致，因此没有重复替换。原用户预览服务 53967（PID 36685）保留，一次性测试实例已结束。官网未部署，未修改真实图标数据；宣传视频与对应账本记录仍留在工作区，未纳入发布。
