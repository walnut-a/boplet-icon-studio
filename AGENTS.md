# 项目工作约定

- 以 README.md、docs/ 中已确认需求及开发计划为基准；后续开发在此仓库内进行，不修改父目录旧候选、全局安装或真实图标项目。
- 默认当前 Agent 自行完成，未经用户明确授权不启动子 Agent。
- 先 Skill 后网页；WebMCP 优先但不是 Skill 使用门槛，两条入口共用业务核心和操作合同。
- 不引入旧数据迁移、项目锁、基础版本冲突拦截、自动合并；同一目标最后成功保存生效。
- 保留已确认界面结构；不增加手工需求表单、几何编辑器、内部逻辑说明或主观评价文案。导出同时支持工具和按钮。
- 可自动验证的行为先写失败测试，再实现并验证；视觉交互改动还须检查真实运行界面。
- docs/开发计划是唯一进度账本（完整文件名见 README 链接）。仅凭建仓、构建或测试不得宣称功能、安装或部署已完成。
- 不提交用户数据、密钥、缓存、日志或构建产物；提交只包含当前任务改动，使用中文说明，不默认推送。
- 任务范围外的全局 Skill 替换、安装及对外发布需要明确授权。不得清理归属不明的进程。
